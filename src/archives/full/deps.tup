# https://gittup.org/tup/manual.html

# The quickjs engine, its dependencies, the global 'inspect' function, and
# quickjs-libc (which defines the "std" and "os" modules, as well as some
# other stuff).

include ../core/deps.tup

DEPS_HOST += $(TUP_CWD)/../../inspect/inspect.host.o
DEPS_HOST += $(TUP_CWD)/../../quickjs-libc/quickjs-libc.host.o

DEPS_TARGET += $(TUP_CWD)/../../inspect/inspect.target.o
DEPS_TARGET += $(TUP_CWD)/../../quickjs-libc/quickjs-libc.target.o
