# https://gittup.org/tup/manual.html
include_rules

CFLAGS += -DCONFIG_VERSION="\"@(VERSION)\""

ifdef BIGNUM
  CFLAGS += -DCONFIG_BIGNUM
endif

ifdef LTO
  CFLAGS += -DCONFIG_LTO
endif

: quickjs.c |> !compile_c_object |> %B.o
