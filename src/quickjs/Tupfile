# https://gittup.org/tup/manual.html

CFLAGS = -DCONFIG_VERSION="\"@(VERSION)\""

ifdef BIGNUM
  CFLAGS += -DCONFIG_BIGNUM
endif

ifdef LTO
  CFLAGS += -DCONFIG_LTO
endif

: quickjs.c |> gcc -Wall -c %f -o %o $(CFLAGS) |> %B.o
