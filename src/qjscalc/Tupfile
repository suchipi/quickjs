# https://gittup.org/tup/manual.html
include_rules

MAYBE_BIGNUM=

ifeq (@(BIGNUM),y)
  MAYBE_BIGNUM = -fbignum
endif

: qjscalc.js | ../qjsc/qjsc |> ../qjsc/qjsc $(MAYBE_BIGNUM) -c -o %o %f |> qjscalc.c
: qjscalc.c |> !compile_c_object |> %B.o
