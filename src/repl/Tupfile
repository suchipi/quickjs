# https://gittup.org/tup/manual.html
include_rules

ifdef WIN32
  QJSC = ../qjsc/qjsc.exe
  QJSC_CMD = wine $(QJSC)
else
  QJSC = ../qjsc/qjsc
  QJSC_CMD = $(QJSC)
endif

: repl.js | $(QJSC) |> $(QJSC_CMD) -c -o %o -m %f |> repl.c
: repl.c |> !compile_c_object |> %B.o
