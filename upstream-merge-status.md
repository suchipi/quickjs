# Upstream Merge Status

Rows ordered oldest → newest. Status is one of: PENDING, PORT, SKIP-NA, SKIP-DONE, SKIP-BAD.

Merge base: `2788d71` ("updated to Unicode 14.0.0"). Target tip at seed time: `d7ae12a` ("added JSON.parse source text access"). Total: 428 commits.

| Short SHA | Status | Subject | Notes |
|-----------|--------|---------|-------|
| 6de52d8 | SKIP-DONE | bf_set_ui() fix (github issue #133) | Already applied in fork: shld/shrd at src/lib/libbf/libbf.c:169-182, bf_set_ui uses shld at :256. |
| 03cc5ec | SKIP-DONE | fixed js_proxy_isArray stack overflow (github issue #178) | Already applied in fork at src/quickjs/quickjs.c:45622-45625. |
| 94010ed | SKIP-BAD | the BigInt support is now always included | Fork wants to keep CONFIG_BIGNUM as a meaningful toggle; do not remove the ifdef guards around BigInt code. |
| cdeca4d | SKIP-DONE | updated to unicode 15.0.0 | Fork is already on Unicode 15 (unicode_download.sh URL, Kawi + Nag_Mundari in unicode_gen_def.h, test262.conf matches). libunicode-table.h is generated from the .txt inputs, not vendored. test262_errors.txt is a fork-specific baseline. |
| 3106401 | SKIP-NA | keep LTO | Makefile only; fork uses Ninja/meta build and doesn't vendor Makefile. |
| f87cab0 | SKIP-DONE | added String.prototype.at, Array.prototype.at and TypedArray.prototype.at | Fork already has js_array_at (:39784), js_string_at (:40759), js_typed_array_at (:52465) in src/quickjs/quickjs.c |
| 321dbfa | PORT | added missing bignum error tests (github issue #159) | Added null checks after JS_ToBigFloat/JS_ToBigDecimal/JS_ToBigInt calls in CONFIG_BIGNUM code |
| 55a4878 | PORT | fixed private field setters (github issue #194) | Fixed scope_put_private_field opcode stack counts (2,0 not 1,1); typo fix unconsistent→inconsistent. OP_drop after setter call_method already present in fork. |
| b14d77b | PORT | fixed negative zero date | Reject negative zero in string_get_signed_digits for Date parsing; 2 test262 errors resolved |
| 26fdf65 | SKIP-DONE | Make Date methods argument coercion spec compliant (bnoordhuis) | Fork already has unconditional argument coercion in set_date_field() at src/quickjs/quickjs.c:48502 |
| c4cdd61 | PORT | fixed lexical scope of 'this' with eval (github issue #192) | Pass vd->is_lexical instead of FALSE in get_closure_var calls in add_eval_variables |
| 4949d75 | SKIP-DONE | Retrieve RegExp 'g' flag in spec conformant way (original patch by bnoordhuis) | Fork already reads flags via JS_ATOM_flags in Symbol.match (:43243) and Symbol.replace |
| 20a57f9 | PORT | Implement extended named capture group identifiers (bnoordhuis) | Extended re_parse_group_name to handle surrogate pairs; removed is_utf16 param. 1 test262 error resolved. |
| 58f374e | PORT | reworked set property and fixed corner cases of typed array set property | Merged JS_SetPropertyGeneric into JS_SetPropertyInternal (new receiver param); callers updated to pass obj+this_obj. Two prior fork pre-ports (4832473, 2bc4159) were reverted first so the port applies cleanly. Items 3 (explicit detach check in JS_DefineProperty), 4 (species), 5 (sort-on-detach) from 4832473 are now re-earned as the corresponding upstream commits get processed. 14 test262 errors fixed; sort-tonumber and no-species still pending upstream b180cd2/b8791e9. |
| e182050 | PORT | fixed delete super.x error | 2-line fix in js_parse_delete: for the OP_get_super_value case, reset byte_code.size and last_opcode_pos before emitting the throw_error opcode so the thrown error is a ReferenceError, not TypeError. 1 test262 error resolved. |
| b180cd2 | PORT | Symbol.species is no longer used in TypedArray constructor from a TypedArray | Removed JS_SpeciesConstructor call in js_typed_array_constructor_ta; per sec-typedarray-create the default %ArrayBuffer% constructor is used unconditionally. 1 test262 error resolved (no-species.js). |
| 177af41 | PORT | fixed duplicate static private setter/getter test | Added is_static_private bit to JSVarDef, threaded is_static through add_private_class_field, and extended the already-defined check in js_parse_class to reject same-name getter+setter pairs on different sides (static vs non-static). Fork's 52e7332 pre-port of this same fix (with different bit name) was partially reverted first to take upstream's shape verbatim for future merges. 4 test262 errors resolved (cross-static early-error tests). |
| a057008 | PORT | added Array.prototype.findLast{Index} and TypeArray.prototype.findLast{index} (initial patch by bnoordhuis) | Added special_find/findIndex/findLast/findLastIndex enum, converted js_array_find/js_typed_array_find to iterate in either direction based on mode, registered new findLast/findLastIndex methods on both prototypes. Updated Array.prototype[Symbol.unscopables] list (merged with fork's existing 'at' entry). Un-skipped test262's array-find-from-last feature in src/run-test262/test262.conf (103 additional tests now run). |
| daa35bc | SKIP-NA | new release | Only updates Changelog and VERSION, neither of which is tracked in the fork. |
| 9e52965 | PORT | raise an error if a private method is added twice to an object | Added the find_own_property check in JS_AddBrand that throws TypeError when the private brand is already installed (matches fork's previous independent pre-port as item 1 of 52e7332; that pre-port was reverted first to take upstream's shape verbatim). Adapted the JS_ThrowTypeError call to include fork's required file/line args. |
| a610598 | PORT | added -fwrapv to CFLAGS to ensure that signed overflows have a well defined behavior | Ported the Makefile CFLAGS change to the fork's Ninja build (meta/ninja/defs.ninja.js) — added `-fwrapv` to both CFLAGS_HOST and CFLAGS_TARGET. The flag alone surfaced a pre-existing OOB-argv read in js_error_constructor triggered by Reflect.construct(Error, []) (fork's custom options-iteration code reads argv[1] without checking argc); added argc guards as part of this commit so the fork doesn't regress. |
| a42681a | PORT | Fix AsyncGenerator.prototype.return error handling (bnoordhuis) | In js_async_generator_completed_return, when js_promise_resolve throws (e.g. poisoned .constructor property), catch the exception and re-route it as a rejection rather than propagating -1. 2 test262 errors resolved (return-state-completed-broken-promise.js, return-suspendedStart-broken-promise.js). |
| 57105c7 | PORT | fixed async generator in case of exception in the implicit await in the 'return' statement | Moved the implicit-await emission in emit_return to before the finally-block drain, so a throwing await is catchable. Also added error handling for js_async_generator_await in js_async_generator_resume_next (FUNC_RET_AWAIT path), and removed an erroneous goto done after js_async_generator_completed_return. 1 test262 error resolved (return-suspendedYield-broken-promise-try-catch.js). |
| 4bb8c35 | PORT | fixed 'return' handling with 'yield' in 'for of' or with finally blocks (gihub ticket #166) | Replaced OP_iterator_close_return with the more general OP_nip_catch, restructured emit_return to emit either nip_catch + iterator_close / nip_catch + gosub finally. Added the new f3 generator regression test to tests/oldtests/test_builtin.js (fork-equivalent of upstream's tests/test_builtin.js addition). Adapted JS_ThrowInternalError calls to fork's file/line convention and clarified "nip_catch" error messages. Regenerated the bytecode snapshot in tests/file-to-bytecode.test.ts (the new opcode inserted at position 184 shifted every subsequent opcode by 1). |
| 9e1ec09 | PORT | test 128 bit integer support (github issue #125) | In src/lib/libbf/libbf.h, the LIMB_LOG2_BITS=6 path requires __int128; also gate on __SIZEOF_INT128__ so compilers without it (e.g. MSVC) fall back to LIMB_LOG2_BITS=5. |
| a96f440 | PORT | fixed js_strtod with large integers (github issue #206) | Fall back to system strtod for base-10 integers that overflow the uint64_t accumulator instead of using pow(10, int_exp), which loses precision. Also added the regression test ((19686109595169230000).toString() === "19686109595169230000") to tests/oldtests/test_language.js (fork-equivalent of upstream's tests/test_language.js). |
| 1692f2a | PORT | safer typed array finalizer | GC can call the ArrayBuffer finalizer before finalizers of typed arrays pointing at it. js_array_buffer_finalizer now walks the abuf->array_list and zeroes each typed array's data pointer + count (non-DataView), plus severs the list link so js_typed_array_finalizer knows not to list_del again. Replaced JS_IsLiveObject(rt, ta->buffer) with the cheaper ta->link.next NULL check. |
| c359951 | PORT | added container_of macro | Added generic container_of(ptr, type, member) macro in src/lib/cutils/cutils.h; reused it from src/lib/list/list.h's list_entry and from JS_FreeCString in quickjs.c. |
| 3ba181e | PORT | fixed define own property with writable=false on module namespace | In JS_DefineProperty's VARREF path: (1) only update the VARREF target when the descriptor isn't the "same value" short-circuit, (2) when flipping a varref to writable=false, throw on JS_CLASS_MODULE_NS since module namespace exports must stay writable (spec). 1 test262 error resolved (define-own-property.js). |
| 4342023 | PORT | removed incorrect await in async yield* | Dropped an extra OP_await that was being emitted before OP_async_yield_star in js_parse_assign_expr2's yield* path; OP_async_yield_star internally performs the await, so the double-await was unwrapping the yielded promise prematurely. 2 test262 errors resolved. |
| 7cefa7b | PORT | 'for of' expression cannot start with 'async' | Reject `for (async of …)` at parse time in js_parse_for_in_of — the spec forbids the unparenthesised `async` token as LHS to `for of` to avoid ambiguity with `for await (… of …)`. 1 test262 error resolved (head-lhs-async-invalid.js). |
| 07ff474 | PORT | use Unicode normalization in String.prototype.localeCompare | Factored out js_string_normalize1 from js_string_normalize and added js_UTF32_compare; js_string_localeCompare now NFC-normalizes both operands before comparing, matching the ECMA-402 spec so ö (U+00F6) equals o+U+0308. Preserved the CONFIG_ALL_UNICODE=0 branch that skips normalization. 1 test262 error resolved. |
| e68993b | PORT | removed unused JSContext field | Removed the unused `is_error_property_enabled` BOOL from struct JSContext. |
| e929040 | PORT | reduced JS_MAX_LOCAL_VARS (github issue #123) | Dropped JS_MAX_LOCAL_VARS from 65536 to 65535 so the JSAtom u16 index space (and a few uint16 counters) can't overflow at the upper boundary. |
| 9b587c4 | SKIP-DONE | call js_std_free_handlers() in the code generated by qjsc (github issue #96) | Fork's qjsc main_c_template2 already calls js_eventloop_free(rt) (src/programs/qjsc/qjsc.c:364), which is the fork's equivalent of upstream's js_std_free_handlers — walks ts->rw_handlers, signal_handlers, and timers and frees each. The Makefile example-target change is SKIP-NA (fork uses Ninja). |
| 6e4931c | PORT | top-level-await support - follow the spec in the implementation of the module linking and evaluation to avoid errors with cycling module dependencies | Two-evaluator split (Zalgo-safe): js_evaluate_module_sync refuses TLA up front via a two-phase graph walk; js_evaluate_module_async is upstream's spec-compliant Tarjan-DFS evaluator. JS_RunModule remains the sync primitive; new JS_LoadModule is the async wrapper. JS_EvalFunctionInternal routes based on whether the graph transitively has TLA — non-TLA code stays sync even when async_modules=TRUE. JS_EvalFunctionAsync exposed alongside JS_EvalFunction. QJMS_Eval*Async wrappers added; qjs / quickjs-run main entry uses async; require/engine.importModule stay sync (throw TypeError with module name). Workers use JS_LoadModule (real TLA support). Added 12 vitest tests (tests/tla.test.ts) covering main-entry, dynamic import, require, engine.importModule, cycles, rejection, and ModuleDelegate preservation. top-level-await feature enabled in test262.conf (253 additional tests enabled, all passing). Bytecode-format change for has_tla flag; file-to-bytecode/libbytecode snapshots regenerated. |
| e6da06e | PORT | typos | qjsc help text "in a C file" → "to a C file" (two places); unicode_gen.c `#undef UNICODE_SPROP_LIST` → `UNICODE_PROP_LIST` (corrects a dangling undef that didn't match the corresponding define). |
| b8791e9 | PORT | fixed detached TypedArray in sort() | In js_TA_cmp_generic, replaced validate_typed_array (which threw) with typed_array_is_detached returning exception=2 so the sort completes without throwing when the comparator detaches the buffer mid-sort. Also moved the comparator-arg check_function call earlier per spec. Adapted to fork's detailed TypeError message. 1 test262 error resolved. |
| 4876f72 | PORT | added String.prototype.isWellFormed and String.prototype.toWellFormed | Clean apply; trivial conflict in js_string_proto_funcs (kept fork's `at` entry alongside upstream's two new entries). Enabled in test262.conf; no change to test262_errors.txt (new tests pass). |
| c2c773e | PORT | added Object.groupBy and Map.groupBy (initial patch by bnoordhuis) | Clean apply; one adaptation — upstream's bare JS_ThrowTypeError in js_object_groupBy needed the fork's file/line args. Enabled `array-grouping` in test262.conf. The test262 feature flag also covers the older (withdrawn) Array.prototype.group / .groupToMap proposals that neither upstream nor this fork implements; 34 "feature not implemented" failures added to test262_errors.txt match upstream's state. |
| feefdb1 | PORT | added Array.prototype.{with,toReversed,toSpliced,toSorted} and TypedArray.prototype.{with,toReversed,toSorted} (initial patch by bnoordhuis) | Seven new protomethods. Three JS_Throw* call sites adapted to fork's file/line signature (js_allocate_fast_array, js_array_toSpliced, js_typed_array_with). Took the opportunity to replace fork's js_array_at and js_typed_array_at with upstream's versions — both are strictly more spec-correct (JS_ToInt64Sat instead of JS_ToInt32/JS_ToInt64, giving saturating semantics for infinities/NaN per ToIntegerOrInfinity) and upstream's js_array_at also has a fast path for dense arrays. The upstream positions for `at` (next to `with` at the top of the proto-funcs tables) are adopted. Enabled `change-array-by-copy` in test262.conf; no change to test262_errors.txt. |
| 5fc27dc | PORT | added Promise.withResolvers | Clean apply; repurposed a previously `#if 0`'d stub (`js_promise___newPromiseCapability`) into `js_promise_withResolvers` — now using `this_val` instead of `argv[0]` as the constructor. Enabled in test262.conf; no regression. |
| a47f40c | PORT | added RegExp 'd' flag (bnoordhuis) | Clean apply across libregexp.h (new LRE_FLAG_INDICES, unused by libregexp — just recorded), quickjs-atom.h (new `indices` atom), quickjs.c (flag parse, hasIndices check in js_regexp_get_flags, full js_regexp_exec rewrite — unified goto-fail cleanup and builds per-capture `indices` array + named-group indices object), and proto_funcs switched to LRE_FLAG_* symbolic constants plus new hasIndices getter. test262.conf: unskipped `regexp-match-indices`. Inserting the `indices` atom shifted all later atom IDs by +1, which broke four bytecode-snapshot tests (libbytecode.test.ts ×3, file-to-bytecode.test.ts ×1) and the hard-coded `log-four.js` bytecode buffer in qjsbootstrap-bytecode.test.ts. Regenerated the buffer and `npx vitest -u`'d the snapshots — pure atom-ID shifts, no semantic change. TODO upstream file: not vendored (skipped). No .d.ts changes — RegExp typings come from TS lib.es, which already includes hasIndices/indices. test262_errors.txt unchanged (new regexp-match-indices tests all pass). |
| 399d916 | PORT | removed memory leak | Clean apply to quickjs.c js_parse_postfix_expr: after `name = JS_DupAtom(...)`, the `next_token()` failure path was returning without freeing `name`. Wrapped the failing branch in `{ JS_FreeAtom(ctx, name); return -1; }`. 3 lines. No test262 error diff. |
| 7414e5f | PORT | fixed the garbage collection of async functions with closures (github issue #156) | Large GC refactor: `JSAsyncFunctionData` absorbed into `JSAsyncFunctionState` (which now owns the GC header + resolving_funcs + is_completed); `JSVarRef` gains a second slot via union — detached refs hold `value`, on-stack refs hold `{ var_ref_link, async_func }`, so closed-over var-refs from async frames hold a ref back to the async state for cycle collection. `async_func_free` split into `async_func_free_frame` / `__async_func_free` / `async_func_free`; `close_var_refs` now runs at end of `async_func_resume` (completed path), not from the frame-freeing function. 3way auto-apply produced 3 conflicts: (1) a fork-preserved stale comment about `header.link` usage that the patch obsoleted — kept patch's removal; (2) a redundant `sf = &s->frame` + inline `close_var_refs` in `async_func_free_frame` that the patch correctly moved elsewhere — took patch; (3) `JS_ThrowTypeError` argc mismatch in js_generator_next (fork uses `(ctx, file, line, msg)`, upstream uses `(ctx, msg)`) combined with `func_state.` → `func_state->` — kept fork's throw signature with upstream's pointer deref. Test_std.js: appended `test_async_gc` to `tests/oldtests/test_std.js` (fork's location) with trailing whitespace stripped per `.editorconfig`. test262_errors.txt unchanged. |
| b4d8050 | PORT | fixed crash when resizing property shapes in case of OOM (github issue #129) | Clean apply to resize_properties in quickjs.c. Problem: the previous code used `js_realloc` in the equal-hash-size branch; a GC triggered during the realloc could see the shape in an invalid intermediate state. Fix always allocates a fresh shape via `js_malloc`, copies the old shape contents, and frees the old after linking the new one into `gc_obj_list`. Single code path now instead of two. No test262 error diff. |
| 2785ede | PORT | fixed JS module autodetection with shebang (github issue #91) | Core intent: teach `JS_DetectModule` to skip shebangs before tokenizing (so `#!...\nimport ...` is still detected as a module). Adapted to fork: the fork already had its own richer `shebangs_end_index`/`skip_shebangs` helpers (multi-shebang support for Nix tooling) and already called `skip_shebangs` in `__JS_EvalInternal`. Took upstream's *call-site change* in `JS_DetectModule` (now skips shebangs before tokenizing) but using the fork's existing `shebangs_end_index`; discarded upstream's new `skip_shebang`/`buf_end` helper as redundant with the fork's superset. 3 apply conflicts, all resolved by "keep fork's multi-shebang helpers, drop upstream's single-shebang duplicate." Added the `#! (shebang test)` line to `tests/oldtests/test_std.js` (fork's path for upstream's `tests/test_std.js`). test262_errors.txt unchanged. |
| ffe8141 | PORT | define the same atoms with or without CONFIG_ATOMICS (github issue #76) | Upstream's intent (keep `not_equal` / `timed_out` / `ok` atoms defined unconditionally so bytecode is portable between builds with and without CONFIG_ATOMICS) is the same principle the fork already follows for bytecode cross-build compatibility. Fork had already dropped the `#ifdef CONFIG_ATOMICS` / `#endif` wrapper around these three DEFs. Kept the fork's shape (bare DEFs) and added a comment explicitly stating the fork-identity rationale (bytecode-format compatibility across SKIP_WORKER builds); dropped upstream's cosmetic `/* */` line. No atom-index shift. test262_errors.txt unchanged. |
| 2ee6be7 | PORT | added os.now() | Split across fork's module layout: (a) added `js_os_now` to `src/builtin-modules/quickjs-os/quickjs-os.c` — returns `(double)gettime_ns() / 1e6` using the fork's existing `src/lib/gettime` helpers; registered `now` in `js_os_funcs`. (b) updated `quickjs-os.d.ts` with a typed export + doc. (c) removed `js___date_clock`, the `#if 0`'d `js___date_now` block, and the `__date_clock` / commented-out `__date_*` entries from `js_global_funcs` in `quickjs.c`. (d) updated fork's `meta/microbench.js` (upstream: `tests/microbench.js`) to import `quickjs:os` and use `os.now` as the clock. (e) updated snapshots in `tests/globals.test.ts` + `tests/libcontext.test.ts` (removed `__date_clock` rows) and dropped the `__date_clock` mention from `quickjs-context.d.ts`. Doc file (`doc/quickjs.texi`) skipped — fork doesn't vendor doc. No test262 error diff. |
| 5c120cd | PORT | added Error cause | quickjs-atom.h: upstream added `DEF(cause, "cause")` after `message`; fork already had one further down (after `stack`). Removed upstream's insertion and kept the fork's existing position so no atom-IDs shift (no snapshot churn). quickjs.c: fork already had an "options" parameter handling — a fork-specific extension that copies ALL enumerable own properties from the options object onto the Error instance with `JS_PROP_C_W_E`. Per ES2022 spec, `cause` must be configurable + writable but NOT enumerable, and `HasProperty` errors must surface. Resolution: kept the fork's generic options-copy for non-`cause` keys (skip `cause` in that loop to avoid triggering its getter twice) and added the spec-compliant `cause`-specific handler afterwards. Fixed the 4 test262 error-cause failures that the naive apply introduced. Updated `tests/error-cause.test.ts` snapshot to match new non-enumerable semantics (cause no longer shows up in default enumeration; descriptor `enumerable: false`). Enabled `error-cause` in test262.conf. Net: 5 new error-cause tests, all pass. TODO file skipped (not vendored). |
| 8de4538 | PORT | make JS_NewClassID thread safe | Clean apply. Adds a `pthread_mutex_t js_class_id_mutex` (CONFIG_ATOMICS-gated) and locks around the class-id allocation in `JS_NewClassID`. No test262 error diff. |
| e44b793 | PORT | allow 'await' in the REPL and added os.sleepAsync() | Split across fork's module split: (a) quickjs.h: added `JS_EVAL_FLAG_ASYNC`. (b) quickjs.c: 2 hunks (emit_return-with-async for eval-result wrapping; extend async func_kind to apply on JS_EVAL_FLAG_ASYNC). (c) quickjs-engine: added an `async` option to `evalScript` (fork's analogue of `js_evalScript`; uses fork's camelCase + typed-option pattern rather than upstream's `get_bool_option`). Updated quickjs-engine.d.ts. (d) quickjs-timers: added `js_timers_sleepAsync` (adapted from upstream's `js_os_sleepAsync` — fork keeps timer APIs in its own `quickjs:timers` module rather than `quickjs:os`, so the export lives at `timers.sleepAsync` not `os.sleepAsync`; this matches the fork's pre-existing reorg of timer functions out of `os`). Updated quickjs-timers.d.ts. (e) repl.js: restructured `handle_cmd` to return truthy when async eval kicked off, `eval_and_print_start(expr, is_async)` does the evalScript call with `async: is_async` and chains `.then(print_eval_result, print_eval_error)`, and `handle_cmd_end` factors out the cleanup+readline_restart. The raw 3way apply misapplied most of the repl hunks into the wrong functions (formatting divergence between fork's prettier style and upstream's K&R), so repl.js was ported by hand. Fork-specific features preserved: `g._error = error`, `engine.evalScript` (not `std.evalScript`), destructure-and-print own-props on errors, camelCase option names. Doc file skipped (not vendored). No test262 error diff. |
| 8f897d6 | PORT | fixed crash in JS_DumpMemoryUsage (github issue #65) | Clean apply. Avoid OOB on dynamic classes in the object-classes dump loop — guard with `class_id < rt->class_count` and read class_name from `rt->class_array` (the live per-runtime table) instead of the static `js_std_class_def` builtin table. No test262 error diff. |
| a8064b7 | SKIP-NA | added note about atomic operations | doc/quickjs.texi only; fork doesn't vendor doc. |
| 1605764 | PORT | class static block (initial patch by bnoordhuis) | Clean apply. Adds `JS_PARSE_FUNC_CLASS_STATIC_INIT`, parser handling for `static { ... }` blocks inside class bodies (emitted as a no-arg method call to a closure with `this` bound to the class), `await` keyword restriction inside static init, `return`-disallowed error, and the surrounding `js_parse_function_decl2` special-cases (no args binding, no `(` parsing). Enabled `class-static-block` in test262.conf: 63 new tests, all pass. TODO skipped. |
| 3ab1c2b | PORT | added 'in' operator for private fields | Added `private_in` opcode (`#field in obj` operator) plus the `scope_in_private_field` scope opcode. Three areas of quickjs.c had conflicts: (1) JS_AddBrand restructured to tolerate non-object `obj` (upstream's refactor, kept fork's file/line JS_ThrowTypeError signature); (2) JS_CheckBrand restructured to return 0/1 and move throw to caller at OP_check_brand opcode (kept fork's signature); (3) emit_class_init_start now gates brand emit on `!cf->is_static` — the fork was already partway to this refactor (had `need_brand`/`is_static` struct fields set at call sites, but `emit_class_init_start` still did unconditional emit and a dead `add_brand()` helper remained). Removed the dead helper and completed the restructure. Also added `cf->need_brand` prototype-brand emission block in js_parse_class (was missing in fork). Two new JS_ThrowTypeError call sites (js_operator_private_in, OP_check_brand) adapted to fork's `(ctx, file, line, msg)` signature. Opcode addition shifted opcode IDs; regenerated qjsbootstrap-bytecode fixture and updated 4 bytecode snapshots (libbytecode ×2, file-to-bytecode ×1, qjsbootstrap-bytecode ×1). Enabled `class-fields-private-in` in test262.conf. 19 new tests pass, 1 expected-failure added (private-field-invalid-assignment-target.js — matches upstream's state). |
| bd0b704 | PORT | added a comment for non-initialized warning in Valgrind (github issue #153) | Clean apply. Documentation comment only — explains why the array-count read in `JS_GetPropertyValue`'s fast path is correct even when Valgrind reports it as uninitialized. |
| 24aa7ba | PORT | fixed test262: derived-this-uninitialized-realm.js | Adds `get_loc_checkthis` / `scope_get_var_checkthis` opcodes so that the uninitialized-`this` ReferenceError in derived class constructors is raised in the caller realm rather than the callee realm. opcode.h conflict was a trivial position question (upstream's ordering adopted; fork's trailing whitespace on the following line dropped). Two new opcodes shifted IDs; regenerated qjsbootstrap-bytecode fixture and updated 4 bytecode snapshots. test262 errors went from 42 → 41 (derived-this-uninitialized-realm.js now passes). |
| df3781d | PORT | make for in faster and spec compliant (github issue #137) | Clean apply to quickjs.c — rewrites the `for..in` iterator to be both spec-compliant (handles mid-iteration property removal via Proxy `getOwnPropertyDescriptor` returning undefined) and faster (new fast path). Added `test_for_in_proxy` to fork's `tests/oldtests/test_loop.js` (upstream path `tests/test_loop.js`). No test262 error diff. |
| 3c2cfab | PORT | fixed run_test262_harness_test() with modules | Adds module support + promise rejection handling to `run_test262_harness_test` in src/run-test262/run-test262.c. Two upstream `js_std_dump_error` calls adapted to fork's `QJU_PrintException(ctx, stderr)` helper (fork's equivalent). Conflict was trivial (fork had tabs where upstream had spaces + differing error-printer name). |
| c363586 | PORT | avoid potentially undefined behavior and make valgrind happy (bnoordhuis) (github issue #153) | Clean apply. Reverses bd0b704's approach: moves the bounds check into each class-id case branch in `JS_GetPropertyValue`'s fast path so `p->u.array.count` is only read for classes that actually have it initialized. Removes the explanatory comment bd0b704 added (no longer needed). |
| 5935a26 | PORT | fixed class name init in static initializers | Moves the `class_name` scoped-variable init (the block that writes the class back to a scoped var for inner self-reference) from after static-fields init to before it, so `static x = S.x` in class S works. Removes the duplicate block that 1605764 had injected into the static-init-closure path (it wrote to the wrong scope level anyway). Conflict was the former duplicate — resolved by deleting it. Added the 3-line `static x/y/z` smoke test to fork's tests/oldtests/test_language.js. |
| c06c399 | PORT | fixed next token parsing after a function definition (github issue #77) | Clean apply to quickjs.c — adds `reparse_ident_token` and fixes post-function-decl token reparsing so that `yield` / `await` after a nested `function` / arrow-without-semicolon are correctly re-classified as keywords. Added test_parse_semicolon to fork's tests/oldtests/test_language.js. |
| aac2464 | PORT | fix worker termination in example (github issue #98) | 1-line addition to fork's tests/oldtests/test_worker_module.js (upstream path tests/test_worker_module.js): set `parent.onMessage = null` after sending the "done" message so the worker terminates cleanly. |
| af30861 | PORT | fixed regexp case insensitive flag | Large port across libregexp.c + libunicode.c + libunicode.h + unicode_gen.c. Refactors `lre_case_conv` into an outer table-walker + new `lre_case_conv_entry` helper. Adds `cr_regexp_canonicalize` (new libunicode API) with supporting `point_cmp`/`cr_sort_and_remove_overlap` helpers. libunicode-table.h (generated) is rebuilt automatically by the ninja unicode_gen step — not checked in. 3 conflicts all trivial: (1) fork's stray pre-patch `is_lower = ...;` line that upstream deleted (took upstream); (2) fork's empty slot where upstream inserted cr_sort_and_remove_overlap + cr_regexp_canonicalize (took upstream); (3) a commented-out `dump_table()` line the fork had vs upstream's new `#ifdef DUMP_CASE_FOLDING_SPECIAL_CASES` (took upstream). test262_errors.txt: upstream removes 2 unicode_full_case_folding entries, but the fork had already fixed them locally — no diff. |
| e1e65ac | PORT | fixed Date.toLocaleString() (kuzmas) | 1-line clean apply. Date.toLocaleString format string fix in `js_date_toString`. |
| f25e5d4 | PORT | optional chaining fixes (github issue #103) | Adds OP_get_field_opt_chain + OP_get_array_el_opt_chain opcodes (for correct `delete a?.b.c` semantics and `(a?.b)()` this-binding). Conflict was a pure addition (fork's empty slot). Added test_optional_chaining to fork's tests/oldtests/test_language.js. 1 test262 error resolved (optional-call-preserves-this.js). |
| 10fc744 | PORT | regexp: fixed the zero advance logic in quantifiers (github issue #158) | Replaces `REOP_bne_char_pos` opcode with simpler `REOP_check_advance` in libregexp. Simplifies `re_check_advance` → `re_need_check_advance` (drops the back_reference capture-bitmap tracking — upstream found it unnecessary). 3 conflicts in libregexp.c (fork pre-change had the old 3-state `ret` logic; took upstream's simpler TRUE/FALSE version). Added zero-length-match smoke tests to fork's tests/oldtests/test_builtin.js. test262 lookahead-quantifier-match-groups entries that upstream removed weren't in the fork's list to begin with. |
| 195c42b | PORT | added os.getpid() | Added `js_os_getpid` to fork's `src/builtin-modules/quickjs-os/quickjs-os.c`, using `GetCurrentProcessId()` on Windows and POSIX `getpid()` elsewhere (wraps the #ifdef inline so all three platform branches share one function, matching the fork's convention for platform-agnostic calls). Registered `getpid` in js_os_funcs and added the typed export to quickjs-os.d.ts. Doc file (doc/quickjs.texi) skipped — not vendored. |
| e66ce48 | PORT | more portable and Windows version for getTimezoneOffset() (github issue #122) | Clean apply. `getTimezoneOffset` now uses `gmtime_r/localtime_r` to compute the offset portably, with a `_WIN32`-specific `_mkgmtime`/`mktime` fallback. |
| c950966 | SKIP-NA | update test results | TODO file only; fork doesn't vendor. |
| e80917b | PORT | fixed uninitialized harnessbuf | Clean apply to run-test262.c — `harnessbuf` is now explicitly NULL-initialized so the harness-loading code doesn't read uninitialized memory when a harness isn't requested. |
| 9a4379d | PORT | native cosmopolitan build | Upstream refactored the `js_*_malloc_usable_size` helpers to `const void *` and plugged them into `JSMallocFunctions` by name (instead of inlining the platform dispatch into the struct). Fork was already `const void *` and already had richer platform coverage (__COSMO__, __wasi__), just still had the old inline-dispatch in the struct literal. Took upstream's struct-literal simplification in both quickjs.c and qjs.c; kept the fork's richer helper body (superset of platforms). Makefile changes skipped — fork uses Ninja/meta, not Makefile. |
| efdb722 | PORT | fixed JS_GetScriptOrModuleName() in direct or indirect eval code | Adds `is_direct_or_indirect_eval` flag to JSFunctionBytecode and makes JS_GetScriptOrModuleName walk past eval frames to find the enclosing script/module filename. Conflict was that the fork had fork-added synthetic-stack-frame handling ahead of the name lookup; combined: wrap the synthetic-frame check inside upstream's new `for(;;)` loop. 1 test262 error resolved. |
| 6e651e8 | SKIP-NA | allow override of PREFIX, CROSS_PREFIX, CFLAGS and LDFLAGS in Makefile (humenda) | Makefile-only; fork uses Ninja/meta. |
| 3f81070 | SKIP-NA | new release | Changelog/VERSION/doc updates; fork doesn't vendor any of those. |
| d6c7d16 | SKIP-NA | update Changelog | Changelog only. |
| 8405876 | SKIP-DONE | added js_std_await() and use it to wait for the evaluation of a module (github issue #219) | Fork already has an equivalent idiomatic API: `QJMS_EvalBufAsync`/`QJMS_EvalFileAsync` (async-aware module evaluators) combined with `js_eventloop_run` that drives the event loop until completion. Worker loader uses the same pattern via `JS_LoadModule` + `QJMS_AttachEntryRejectionHandler` + `js_eventloop_run`. Top-level-await in the entry module is already supported end-to-end, which is the behavior upstream's `js_std_await` introduces. |
| 9e561d5 | SKIP-BAD | fixed and simplified setTimeout() by using an integer timer handle (github issue #218) | Fork deliberately uses an opaque JS Timer object handle (JSTimer class with `[Symbol.toStringTag]: "Timer"`, exported as a typed `Timer` from `quickjs:timers`), matching WHATWG/Node.js convention. Accepting upstream's integer-id API would regress visible JS behavior. The underlying "bug" (GC of unreachable timers) isn't an issue in the fork — the JSTimer class owns its lifetime via the finalizer + event-loop linkage. |
| 67723c9 | SKIP-DONE | fixed js_std_await() in case 'obj' is not a promise (github issue #222) | Follow-up to 8405876 which was already SKIP-DONE in the fork; fork doesn't have js_std_await. The fork's `js_eventloop_run` equivalent already exits correctly when the awaited value isn't a promise. |
| 090685a | SKIP-NA | update test results | TODO only. |
| cd666a8 | PORT | simplified and fixed arrow function parsing (github issue #226) | Removes the `PF_ARROW_FUNC` parse_flags bit (arrow functions are now parsed unconditionally in the postfix expression path; the flag had been used to suppress arrow parsing in `??`-coalesce RHS and was the root cause of the test_parse_arrow_function edge cases). Two conflicts in quickjs.c (the flag definition reordering and the call-site where `& ~PF_ARROW_FUNC` was stripped) both resolved by taking upstream's version. Added test_parse_arrow_function to fork's tests/oldtests/test_language.js. |
| c6cc6a9 | PORT | export JS_GetModuleNamespace (github issue #34) | Clean apply of the rename. One additional fork-specific call site (inside the JS_RunModule wrapper that returns the namespace) used the old `js_get_module_ns` internal name and had to be updated to the new public `JS_GetModuleNamespace`. |
| 00967aa | PORT | fixed Promise return in the REPL by using a wrapper object in async std.evalScript() (github issue #231) | Clean apply on quickjs.c (wrap async eval return value in `{ value: ... }` at the bytecode-emission level). repl.js hand-ported (3way apply misrouted into `parse_string`): sync eval passes `{ value: result }` to `print_eval_result`, which unwraps via `result = result.value`. Makefile + doc changes skipped — not vendored. |
| 1ed38ee | SKIP-NA | fixed MingW64 install on Windows (absop) (github issue #230) | Makefile only; fork uses Ninja/meta. |
| 6f480ab | PORT | avoid using INT64_MAX in double comparisons because it cannot be exactly represented as a double (bnoordhuis) | Replaces `d > INT64_MAX` with `d >= 0x1p63` at two sites (JS_ToInt64SatFree and js_atomics_wait). `INT64_MAX` can't be represented exactly as a double (it's 2^63 - 1; the nearest double is 2^63); `0x1p63` is exactly 2^63 and is what's actually wanted as the upper bound. This also sidesteps the `-Wimplicit-int-float-conversion` clang warning, so the fork's surrounding `suppress_warning`/`unsuppress_warning` scaffolding became vestigial and was removed (verified across `meta/build.sh all` — no warning fires on INT64_MIN either, since it's exactly -2^63 and representable). |
| 37bd4ae | SKIP-DONE | Strip trailing spaces | Pure whitespace-normalization commit. Fork's `.editorconfig` sets `trim_trailing_whitespace = true`; all previously ported commits strip trailing whitespace from added lines, so the fork's tree is already in the post-commit state. |
| 2c793e5 | PORT | Fix test262o error | Clean apply to fork's `src/run-test262/test262o.conf`: excludes `test262o/test/suite/ch15/15.5/15.5.4/15.5.4.9/15.5.4.9_CE.js` (locale-comparison test requiring canonical-form equivalence between `ö` and `o+combining-diaeresis`). |
| c9e6c56 | PORT | Improve microbench | Hand-ported to fork's `meta/microbench.js` (kept fork's prettier formatting + `allTests = { ... }` object-based test registry). Ported the substantive changes: 3 new loop tests (`empty_down_loop`, `empty_down_loop2`, `empty_do_loop`), 2 new string-build tests (`string_build1x`, `string_build2c`), removed unused `r` parameter on `string_build3`, and fixed the sort_bench timing log (`log_one("sort_"+f.name, 1, ti/100)` and `total / n / 100`). Kept fork's `quickjs:std`/`quickjs:os` imports (always available in fork's module-based invocation, so upstream's `typeof(os) !== "undefined"` guard and node-compat tweaks are unnecessary in the fork). Skipped `-r ref_file` arg + name-prefix matching — fork's `main(options)` uses an options object, not argv parsing, so those CLI changes don't map. Skipped the sort_bench reordering — fork keeps it in the middle of the list. Makefile and `.gitignore` changes skipped (fork uses Ninja/meta and has its own .gitignore). |
| 48deab1 | PORT | Fix runtime bugs | Two real bug fixes + formatting/comment churn. Ported the fmt_str leak fix in `js_printf_internal` (fork's analogue in `src/builtin-modules/quickjs-std/quickjs-std.c`): initialize `fmt_str = NULL` and add `JS_FreeCString(ctx, fmt_str)` to the fail path. The errno-read-order fix in `js_os_stat` was unnecessary — fork's `src/builtin-modules/quickjs-os/quickjs-os.c` already reads `err = errno;` before any `JS_FreeCString(ctx, path)` or other side effect. Skipped the pure formatting/comment-polish changes in quickjs.c and quickjs-libc.c function signatures. |
| 2e10134 | PORT | Add more tests | Added `regexp_ascii` + `regexp_utf16` microbench tests to fork's `meta/microbench.js` + registered them in `allTests`. Skipped the `bjson_test_regexp` addition because the fork doesn't vendor `test_bjson.js` or `bjson.c` — it uses its own `quickjs:bytecode` module. |
| 626e0d4 | SKIP-NA | Unbroke tests/test_test_bjson.js | Reverts the bjson_test_regexp added in 2e10134. Fork doesn't vendor test_bjson.js at all and skipped the addition in the prior commit, so nothing to revert. |
| 325ca19 | SKIP-NA | Add MemorySanitizer support | Makefile only. |
| fd6e039 | SKIP-NA | Add UndefinedBehaviorSanitizer support | Makefile only. |
| e53d622 | PORT | Fix UB in js_dtoa1 | Clean apply to quickjs.c. Guards against infinite-digit-count UB in the dtoa code path. |
| 6535064 | PORT | Fix undefined behavior (UBSAN) | Moves `sf = &s->func_state->frame` assignment in `js_generator_next` from the top of the function into each switch case that actually uses sf — avoids dereferencing `func_state` (which is NULL in JS_GENERATOR_STATE_COMPLETED / AWAITING_RETURN) before the state check. Kept fork's `(ctx, file, line, msg)` JS_ThrowTypeError signature. .gitignore skipped. |
| 6dbf01b | PORT | Remove unsafe sprintf() and strcat() calls | Clean apply. Replaces deprecated macOS-unsafe sprintf/strcat with snprintf/strncat variants. |
| e140122 | PORT | Fix sloppy mode arguments uninitialized value use | Clean apply. Adds MSan cleanup path for JS_CLASS_MAPPED_ARGUMENTS in the fast-path indexed-property code. |
| 693449e | SKIP-NA | add gitignore for build objects (#84) | .gitignore only; fork has its own. |
| ae6fa8d | SKIP-DONE | Fix shell injection bug in std.urlGet (#61) | Upstream's fix escapes shell metacharacters before `popen("curl ...")`. Fork has already replaced the popen approach entirely with a direct libcurl integration (`qjs_libcurl.easy_init()` etc.) — no shell invocation, so no shell-injection possible. Removed the now-orphaned `URL_GET_PROGRAM`/`URL_GET_BUF_SIZE` macros the 3way apply left behind. Classified SKIP-DONE because fork's approach strictly supersedes upstream's escape-based mitigation. |
| 636c946 | PORT | FreeBSD QuickJS Patch (#203) | qjs.c `#include <malloc_np.h>` on FreeBSD — clean apply. Fork's `quickjs-os.c` already has richer FreeBSD support (`extern char **environ`, custom `sighandler_t` typedef + `signal()` prototype) so no changes there. Makefile changes skipped — fork uses Ninja/meta. |
| 92e339d | SKIP-DONE | Simplify and clarify URL quoting js_std_urlGet | Follow-up to ae6fa8d which was SKIP-DONE because the fork uses direct libcurl (no shell invocation). Fork's urlGet has no quoting code to simplify. |
| ef4e7b2 | PORT | Fix compiler warnings | Guards the unused-for-non-dump `cw_count`/`cw_len_count`/`cw_start` declarations in unicode_gen.c's build_general_category_table / build_script_table / build_script_ext_table with `#ifdef DUMP_TABLE_SIZE`. Conflicts were that the fork used `__maybe_unused` instead; took upstream's ifdef (consistent with how the *uses* of those variables are already gated). Conflict 4 (build_cc_table) combined upstream's new `block_end_pos` outer decl with the removal of fork's outer `__maybe_unused int cc_table_len` (cc_table_len moved entirely inside the DUMP_TABLE_SIZE ifdef where all its uses already live). |
| 1fe0414 | PORT | Fix test262 error | Clean apply. Forces evaluation order in set_date_fields so the Date/UTC/fp-evaluation-order.js test passes. 1 test262 error resolved. |
| 95e0aa0 | PORT | Reverse e140122202cc24728b394f8f90fa2f4a2d7c397e | Clean apply. Reverts e140122 because c363586 removed the need (the per-case bounds check replaced the need for array.count zeroing). |
| 8e21b96 | SKIP-NA | pass node-js command line arguments to microbench | Adds node-compat for the scriptArgs fallback in upstream's `main(argc, argv, g)`. Fork's microbench uses `main(options)` with an options object (not argv parsing), so there's no scriptArgs path to bridge to Node. |
| c06af87 | PORT | Improve string concatenation hack | Adds `JS_ConcatStringInPlace` — appends to a refcount-1 string in its own allocation slack when possible (big win on `r += "x"` microbench loops). Conflict was just context alignment in the preamble (upstream inserted the helper before JS_ConcatString). Makefile/VERSION changes skipped — not vendored. |
| 3bb2ca3 | PORT | Remove unnecessary ssize_t posix-ism | Clean apply. Removes an unnecessary `(ssize_t)` cast in JS_ComputeMemoryUsage. |
| 8df4327 | PORT | Fix UB left shift of negative number | Clean apply — masks negative-number left-shift in bf pow-of-10 path. |
| 85fb2ca | PORT | Fix UB signed integer overflow in js_math_imul | Clean apply to quickjs.c — uses uint32_t arithmetic + standard-conformant conversion. Added 4 new imul test cases to fork's tests/oldtests/test_builtin.js. Makefile changes skipped. |
| 74bdb49 | PORT | Improve tests | Ported the substantive test tweaks: `Math.hypot(...) == N` → `assert(Math.hypot(...), N)` (proper assert form), expanded `test_date` tolerances to accept both QuickJS and Node rounding for `.1235Z` and `.9999Z` cases, and gated `test_argument_scope`'s `eval("var arguments")` SyntaxError check inside a `"use strict"` IIFE (non-strict mode doesn't throw in Node). Skipped the test_bignum.js → test_bigfloat.js split — fork runs bignum tests via vitest (`oldtests.test.ts`) and the split would require also updating the vitest runner, which is reorganization for reorganization's sake. Makefile changes skipped. |
| 0a361b7 | SKIP-NA | handle missing test262 gracefully | Makefile only. |
| 530ba6a | SKIP-NA | handle missing test262 gracefully | .gitignore only. |
| bbf36d5 | PORT | Fix big endian serialization | Removes WORDS_ENDIAN (unconditionally undef'd in cutils.h), and uses `bc_put_u32/u64` / `bc_get_u32/u64` in JS_WriteBigInt / JS_ReadBigInt. Adds host-endian handling in bc_get_u16/u32/u64 and JS_ReadFunctionBytecode. Two conflicts: (a) BCTagEnum — fork had `BC_TAG_ERROR_OBJECT` as the last entry; upstream added `BC_TAG_BIG_FLOAT`/`BC_TAG_BIG_DECIMAL` under CONFIG_BIGNUM. Combined: kept fork's ERROR_OBJECT and appended upstream's two BIGNUM tags. Kept fork's `BC_BASE_VERSION`/`BC_BE_VERSION` bit-flag structure (more flexible than upstream's hardcoded 0x43). (b) bc_tag_name table — same combine. Regenerated qjsbootstrap-bytecode fixture + updated 4 bytecode snapshots (one byte shifted from `14` to `12` at a function-header position — encoding tweak from the refactor, not a tag-value shift). |
| c24a865 | PORT | Improve run-test262 | Adds -t (timings), -C (compact progress), default compact when not a TTY, 2MB agent stack, relative-to-current-path module resolution, -d/-f ignore testdir, and a nonzero return on errors changes. Conflict resolved on the module-loader setup: kept fork's three-call API (`JS_SetModuleNormalizeFunc` + `JS_SetModuleLoaderFunc` + `JS_SetModuleLoaderOpaque`) instead of upstream's single `JS_SetModuleLoaderFunc` with multiple args — the fork's module loader API is deliberately split that way. Pass `(void *)filename` as the module-loader opaque so the relative-path resolution works. **Note:** the new "nonzero return on errors changes" logic means `src/run-test262/run.sh -u` now always exits nonzero (update mode sets `error_file = NULL`, so every expected error counts as "new" for the exit-code check). Non-`-u` invocation still returns 0 when errors match. Error file diff is the source of truth for regression checking. |
| 97ae6f3 | SKIP-NA | Add benchmarks target | Makefile + .gitignore only. |
| 8d932de | PORT | Rename regex flag and field utf16 -> unicode | Clean apply across libregexp.c, libregexp.h, quickjs.c. Renames `LRE_FLAG_UTF16` → `LRE_FLAG_UNICODE` and struct field `is_utf16` → `is_unicode`. |
| 12c91df | PORT | Improve surrogate handling readability | Clean apply across cutils.h, libregexp.c, quickjs.c. Adds inline helpers `is_surrogate`, `is_hi_surrogate`, `is_lo_surrogate`, `get_hi_surrogate`, `get_lo_surrogate`, `from_surrogate`; adds BC header offset/length names in libregexp.c; fixes strict-aliasing violations in `lre_exec_backtrack`. |
| b91a2ae | PORT | Add C API function JS_GetClassID() | Clean apply. Adds `JS_GetClassID(val)` accessor + `JS_INVALID_CLASS_ID` constant. |
| b70e764 | PORT | Rewrite `set_date_fields` to match the ECMA specification | quickjs.c clean apply (double arithmetic, `volatile` to prevent FMA, reject border cases, avoid double→int64 UB). tests/test_builtin.js hand-ported to fork's `tests/oldtests/test_builtin.js` (keep fork's prettier formatting, dedup conflict with prior port's `assert` definition, adopt upstream's new `get_full_type`/NaN/signed-zero-aware `assert` + `throw_error` infrastructure, add new parseFloat tests and Win32/Cygwin skip-guard, add Date.UTC boundary tests). Makefile changes skipped. |
| 27928ce | PORT | Fix Map hash bug | Clean apply to quickjs.c — `map_hash_key` now generates the same hash for JS_INT and JS_FLOAT64 with the same value. Added Map hash-collision tests (int/float equivalence + BigInt) to fork's `tests/oldtests/test_builtin.js`. |
| 6428ce0 | SKIP-DONE | show readable representation of Date objects in repl | Fork's repl uses `inspect(a, ...)` (fork-added rich inspector) instead of upstream's inline type switch. `inspect` already renders Date objects readably — the upstream special-case is subsumed. |
| 78db49c | PORT | Improve Date.parse | Clean apply to quickjs.c (529-line rewrite with separate parsers, spec-compliant NaN on out-of-bounds, up-to-9-decimal millisecond fractions, many new formats). Hand-ported tests/oldtests/test_builtin.js updates (new Date.parse assertions: empty string, various partial formats, "Jan 1 2000" with/without weekday and timezone offsets). Merged with fork's prettier formatting. |
| 8180d3d | PORT | Improve microbench.js | Added the 5 new benchmark functions upstream introduced (`date_parse`, `prop_update`, `prop_clone`, `array_slice`, `global_func_call`) to fork's `meta/microbench.js`, registered them in `allTests`. Renamed local helper `g(a)` → `g_bench(a)` to avoid collision with fork's other uses of `g`. Skipped the timing-infrastructure changes (performance timer via node, reference-file save/load over node, -s option, threshold tweaks), the reformulation of existing benchmarks to use a shared `ref.slice()` baseline, the closure_var → func_closure_call rename, and the Makefile changes — fork runs microbench from its module-based `main(options)` entry and doesn't need those infra changes. |
| a78d2cb | PORT | Improve repl regexp handling | Hand-ported the substantive change (repl completion now treats trailing-after-`/` text as regexp flags and returns `new RegExp('', base)` so the flag set can complete). Skipped the jscalc config reorganization — upstream restructured where `styles` and `ps1` are set (single default + reassigned inside `if (config_numcalc)`), but fork's formatting-diverged version makes the 3way apply produce multi-hundred-line conflicts. The fork's config_numcalc branch is basically dead code (fork doesn't build jscalc), so the reorg has no behavioral impact on the fork. |
| 8d64731 | PORT | Improve Number.prototype.toString for radix other than 10 | Clean apply to quickjs.c — fixes integer/exact-fraction conversions for non-decimal radix, adds approximate fallback for inexact floats, bypasses floating-point for JS_TAG_INT values, avoids divisions for base-10 integer conversions. |
| 35b7b3c | PORT | Improve Date.parse | Clean apply to quickjs.c (case-insensitive month/tz names, AM/PM markers, US timezone names, skip parenthesized text, many v8 format-compatibility fixes). Hand-ported the test additions to fork's tests/oldtests/test_builtin.js: reordered the Date.UTC NaN/non-NaN blocks (matches upstream's ordering) and added two MakeTime/MakeDate precision tests derived from test262/fp-evaluation-order.js, guarded under non-win32/cygwin. |
| 3dd93eb | SKIP-DONE | fix microbench when microbench.txt is missing (#246) | Fork's microbench `load_result` already handles the missing-file case via try/catch around `std.open`. Upstream's bug was calling `f.readAsString()` after `!f` without a return/else; fork's approach isn't affected. |
| 06c100c | PORT | Prevent UB on memcpy and floating point conversions | Clean apply across cutils.h/cutils.c (new `memcpy_no_ub` that tolerates null pointers for 0 count), libbf.c (one use), and quickjs.c/quickjs.h (safer int-range tests in JS_NewFloat64 / JS_ToArrayLengthFree / js_typed_array_indexOf; prevent 0-length alloc in js_worker_postMessage). |
| e17cb9f | SKIP-DONE | Add github CI tests | `.github/workflows/ci.yml` — fork has its own CI via vitest. The test_std.js isatty change is already handled in the fork: fork's tests/oldtests/test_std.js asserts `!os.isatty(0)` (vitest spawns via first-base, so stdin is never a tty). |
| 1a5333b | PORT | prevent 0 length allocation in `js_worker_postMessage` | Applied the `sab_tab_len > 0` guard to fork's `src/builtin-modules/quickjs-os/quickjs-os.c`, which has **two** js_worker_postMessage-adjacent allocation sites (the fork-added error-pipe worker_send_error + the regular js_worker_postMessage). Both updated. 06c100c already patched upstream's single site in quickjs.c, but the fork's OS-module sites are distinct. |
| ebe7496 | PORT | Fix build: use LRE_BOOL in libunicode.h (#244) | Clean apply. Replaces `bool` with `LRE_BOOL` in two function signatures so libunicode.h doesn't require stdbool.h for consumers. |
| 6a89d7c | PORT | Add CI targets, fix test_std.js (#247) | Ported the SIGQUIT → SIGTERM change in `tests/oldtests/test_std.js` (SIGQUIT doesn't work reliably under QEMU), including the added nonzero-status assertion. Skipped the isatty change — fork's test already uses `!os.isatty(0)` which works unconditionally in the fork's vitest-spawned environment. CI yml skipped (fork has its own CI). |
| 65ecb0b | PORT | Improve Date.parse, small fixes | cutils.h + quickjs.h clean apply. quickjs.c: two conflicts. (1) set_date_field: fork had a more elaborate "buffer ToFloat64 results in `values[]` then check `!any_nonfinite` before copying into fields" structure (fork-added defensive code with spec-ordering comments); upstream's simpler version writes to fields unconditionally and relies on set_date_fields's internal NaN handling (from b70e764). Kept fork's explicit structure — functionally equivalent, more readable. (2) Array.prototype[Symbol.unscopables]: combined upstream's `static` keyword with fork's `"at" "\0"` entry (fork-added ES2022 support). Updated `tests/libcontext.test.ts` snapshot to include the new `@@toStringTag: "global"` property on globalThis. |
| 0665131 | PORT | Fix compilation with -DCONFIG_BIGNUM | Clean apply to libbf.c. quickjs.c: disable BigDecimal conversion in JS_ReadBigNum + misc error message fixes. Two minor conflicts in JS_ThrowRangeError sites where fork uses file/line args and upstream lowered case — kept fork's signature + upstream's BigInt capitalization. |
| c0e67c4 | PORT | Simplify redundant initializers for `JS_NewBool()` | Clean-ish apply. Drops unnecessary `BOOL res;` initialization patterns in quickjs.c and the `(isatty(fd) != 0)` redundancy in os.isatty. Two JS_ThrowTypeError conflicts in BigInt code (fork's file/line sig + upstream's capitalization fixes). |
| ce6b6dc | PORT | Use more explicit magic values for array methods | Clean apply. Replaces magic-number constants in js_array_funcs/proto_funcs with named special_indexOf/special_find etc. enums. |
| 203fe2d | PORT | Improve `JSON.stringify` | Adopts upstream's spec-compliant boxed-BigInt handling (delegate to primitive via toJSON-aware code path rather than early-throw) and new error message "Do not know how to serialize a BigInt". Two conflicts at the old fork-added BigInt-forbidden early throws — resolved by taking upstream's restructure while keeping fork's JS_ThrowTypeError file/line signature. |
| 653b227 | PORT | Improve error handling | Adopts upstream's error-type/message upgrades (InternalError → RangeError for too-many-arguments, out-of-bound → invalid array index, regex → regexp, string-too-long → invalid string length). Five conflicts all resolved by combining fork's `(ctx, file, line, fmt, ...)` signature with upstream's new error type + message. |
| 3b45d15 | PORT | Fix endianness handling in `js_dataview_getValue` / `js_dataview_setValue` | Clean apply. Honors the optional littleEndian argument correctly for get/set DataView operations. |
| 1402478 | PORT | Improve unicode table handling (#286) | Clean apply to libunicode.c + unicode_gen.c. libunicode-table.h (generated) is regenerated by the unicode_gen build step. Adds format docs, size stats, and fixes a UBSAN issue in `get_le24()`. |
| 7a2c6f4 | PORT | Improve libunicode and libregexp headers (#288) | Clean apply across libregexp.c/.h, libunicode.c/.h, unicode_gen.c, quickjs.c. Moves `lre_xxx` utility functions into libunicode, introduces `lre_ctype_bits` flags table, simplifies `lre_is_space` / `lre_js_is_ident_first` / `lre_js_is_ident_next` / `simple_next_token` / `is_let`. |
| d9c699f | PORT | fix class method with name get (#258) | Clean apply to quickjs.c (also accept `=` as a non-reserved-ident-follower in js_parse_property_name). Added the 2 new test cases to fork's tests/oldtests/test_language.js. |
| 0c8feca | PORT | Improve class parser (#289) | Clean apply to quickjs.c — `static` as class-member name (field or method). Added the new test cases to fork's tests/oldtests/test_language.js. |
| 01454ca | SKIP-NA | OSS-Fuzz targets improvements (#267) | fuzz/ is not vendored in fork. Makefile changes skipped. |
| 6c43013 | PORT | Add `JS_NewTypedArray()` (#272) | Clean patch apply. Added fork's file/line args to the one new `JS_ThrowRangeError` call (upstream used the 2-arg form). |
| db9dbd0 | PORT | Add `JS_HasException()` (#265) | Clean apply to quickjs.h. quickjs.c conflict was the fork-added `JS_AddPropertyToException` being adjacent to where upstream's `JS_HasException` inserts — kept both functions side-by-side. |
| d53aafe | SKIP-NA | Add the missing fuzz_common.c (#292) | fuzz/ not vendored. |
| 6f9d05f | PORT | Expose `JS_SetUncatchableError()` (#262) | Clean apply. Makes `JS_SetUncatchableError()` a public API (moved from quickjs.c to quickjs.h). |
| f3f2f42 | PORT | Add `JS_StrictEq()`, `JS_SameValue()`, and `JS_SameValueZero()` (#264) | Clean apply to quickjs.c; one conflict in quickjs.h resolved by keeping both upstream's three new JS_BOOL declarations and the fork's existing JS_ToBool comment/decl. No test changes; test262 unchanged at 38/40125. |
| 97be5a3 | PORT | Add `js_resolve_proxy` (#293) | Clean apply. Refactor of JS_IsArray to walk the proxy chain via a new js_resolve_proxy helper (depth-1000 cap), replacing the old js_proxy_isArray recursion. No test262 regression. |
| d378a9f | PORT | Improve `js_os_exec` (#295) | In fork's quickjs-os.c Unix vfork path: tightened fd_max cap from 65536 to 1024 (per upstream, to avoid costly loop on Alpine's sysconf=1048576), and added `#if defined(HAVE_CLOSEFROM)` branch that calls `closefrom(3)` when available. The macOS `/dev/fd` path is left alone (already efficient; HAVE_CLOSEFROM isn't defined on macOS anyway). Makefile feature-detection part SKIP-NA — fork uses Ninja; HAVE_CLOSEFROM scaffolding in place but no detection wired up yet (the branch is inert). `compat/test-closefrom.c` also SKIP-NA since it's a Makefile probe. |
| adec734 | SKIP-NA | fixed test of test262 directory | Makefile-only: fixes a `wildcard` probe in upstream's Makefile targets (test262o → test262). Fork uses Ninja and doesn't vendor the Makefile. |
| d86aaf0 | PORT | updated test262.patch | Fork's src/run-test262/test262.patch was stale relative to fork's test262 checkout (same staleness upstream was fixing). Replaced with upstream's post-commit content verbatim — applies cleanly to fork's checkout. No test262 regression. |
| 36911f0 | PORT | regexp: fix non greedy quantizers with zero length matches | Clean apply of libregexp.c hunk (moved zero-advance-check emission out of the `if (greedy)` branch so non-greedy quantifiers get the same check). Added regression test `/()*?a/.exec(",")` → null to tests/oldtests/test_builtin.js. No test262 regression. |
| b3715f7 | PORT | Fix GC leak in `js_proxy_get()` (#302) | Clean apply. Frees `ret` on the error path after JS_GetOwnPropertyInternal returns < 0. |
| 5417ab0 | PORT | Fix `JS_HasException()` when `null` is thrown (#313) | Replaced JS_NULL with JS_UNINITIALIZED as the "no exception pending" sentinel at 4 clearing sites and in JS_HasException so `throw null` is distinguishable from no-exception. Conflict in JS_NewRuntime2 resolved by keeping fork's `user_opaque_val = JS_NULL` init line next to the upstream-updated current_exception init. |
| 3489493 | PORT | Use malloc_usable_size() on any OS based on GNU libc | Added `defined(__GLIBC__)` to the platform guards that select `<malloc.h>` + `malloc_usable_size()` in both src/quickjs/quickjs.c and src/programs/qjs/qjs.c. Merged with fork's existing `__wasi__` addition; kept fork's cast-free call shape. |
| 8624b5c | PORT | Use ftello() & fseeko() on any OS based on GNU libc | Added `defined(__GLIBC__)` to the two `#if defined(__linux__)` guards around `ftello`/`fseeko` in fork's quickjs-std.c (the fork-equivalent of upstream's quickjs-libc.c for std file I/O). |
| 012451d | PORT | Define a fallback PATH_MAX if not available | Added `#if !defined(PATH_MAX) #define PATH_MAX 4096 #endif` to the two fork files that use PATH_MAX: src/builtin-modules/quickjs-os/quickjs-os.c (upstream's site — fork-equivalent of quickjs-libc.c) and src/lib/execpath/execpath.c (fork-added, uses PATH_MAX too and needed the same Hurd-build fallback). |
| 6e2e68f | PORT | Fix termination in Worker test | Typo fix in tests/oldtests/test_worker_module.js: `parent.onMessage = null` → `parent.onmessage = null` (the property name is lowercase; the wrong case silently fails to terminate the worker). |
| 030333c | PORT | fixed date parsing in case there is more than nine initial digits (initial patch by nickva) | Clean apply. Added overflow guard in string_get_digits (returns FALSE when v ≥ 1e8 before the next multiply) and switched the two string_get_tzoffset / js_date_parse_otherstring call sites from max_digits=9 to max_digits=0 (unbounded, bounded by the new overflow check). |
| 6474793 | PORT | JS_SetPropertyInternal(): avoid recursing thru the prototypes if the property is found in a prototype | Clean apply. Adds `break` in the prototype-walk loop when a writable property is found — stops the recursion once we know the shadowing write is permitted. |
| c739deb | PORT | microbench: use toFixed() | Removed the hand-rolled toPrec() helper from meta/microbench.js and replaced its one call site with `a.toFixed(precs[i])`. |
| 027f3cb | PORT | fix crash when add_property() fails on build arguments (penneryu) | Clean apply. Guards the two `add_property(..., JS_ATOM_length, ...)` calls in js_build_arguments / js_build_mapped_arguments against OOM returning NULL (previously dereferenced `pr->u.value` unconditionally). Refactored js_build_arguments's early-malloc failure to use a shared `fail:` cleanup. |
| 25aaa77 | PORT | allow regexp interruption (e.g. with Ctrl-C in the REPL) | libregexp.c + libregexp.h clean apply (adds LRE_RET_MEMORY_ERROR/LRE_RET_TIMEOUT, lre_poll_timeout counter, and lre_check_timeout callback). quickjs.c conflicts resolved to take upstream's JS_ThrowInterrupted helper + LRE_RET_TIMEOUT branching while keeping fork's JS_ThrowInternalError file/line convention. Fixed trailing whitespace on one line of the new lre_check_timeout body. |
| dfd9c93 | PORT | added missing stack overflow check in JSON.stringify() | Clean apply. Adds js_check_stack_overflow + JS_ThrowStackOverflow at the top of js_json_to_str so deeply-nested values throw cleanly instead of C-stack overflowing. |
| 9bd10d8 | PORT | simplified the handling of closures | Clean apply. Dropped `is_arg` / `var_idx` fields from JSVarRef (now derived from `pvalue` directly); close_lexical_var's signature loses the `is_arg` parameter. get_var_ref's list walk now keys on pvalue identity. |
| 1be68b3 | PORT | fixed CONFIG_ALL_UNICODE compilation | Clean apply. Moved unicode_case1, CASE_U/L/F macros, point_cmp, cr_sort_and_remove_overlap, and cr_regexp_canonicalize from inside `#ifdef CONFIG_ALL_UNICODE` to before it, so regexp case-folding compiles with CONFIG_ALL_UNICODE disabled. |
| 837a697 | PORT | regexp: allow [\-] in unicode mode (#373) | Clean apply. In get_class_atom's escape switch, added an explicit case for '-' that throws invalid_escape when `!inclass && s->is_unicode` — accepts `[\-]` inside a class, rejects bare `\-` in unicode mode. 1 test262 error resolved (unicode_restricted_identity_escape.js). |
| 61e8b94 | PORT | removed bignum support and qjscalc - added optimized BigInt implementation | Seven-phase port (a0d7bb5 → this commit). Removed CLI/option entry points: `--bignum`/`--qjscalc`/`-fbignum` flags and the `qjsc_qjscalc` extern + qjscalc bytecode loader and `src/programs/qjscalc/` directory; removed `bigfloat`/`bigdecimal`/`operators`/`useMath` quickjs:context options. Removed `-DCONFIG_BIGNUM` define and ripped out 119 dead `#ifdef CONFIG_BIGNUM` blocks (3738 lines) from quickjs.c. Adopted upstream's BigInt implementation wholesale: new `JS_TAG_SHORT_BIG_INT=7`, `JS_TAG_FLOAT64` moved 7→8, `JS_LIMB_BITS`/`JS_SHORT_BIG_INT_BITS`, `__JS_NewShortBigInt` across all three JSValue layouts, `JSBigInt` struct + ~3900 lines of arithmetic helpers (`js_bigint_*`), new `js_compare_bigint` (replaces fork's `js_compare_bigfloat`), new `JS_WriteBigInt`/`JS_ReadBigInt` BC serializers, new `push_bigint_i32` opcode + executor case, `BC_BASE_VERSION 3→4`, `float64_as_uint64`/`uint64_as_float64` helpers in cutils.h. Deleted bignum atoms, BIG_FLOAT/FLOAT_ENV/BIG_DECIMAL/OPERATOR_SET class ids, BC_TAG_BIG_FLOAT/BC_TAG_BIG_DECIMAL serialization tags, `JS_IsBigFloat`/`JS_IsBigDecimal` helpers. Deleted `src/lib/libbf/` and removed libbf objects from `src/archives/core.ninja.js`. Kept `JS_AddIntrinsicBigFloat`/`JS_AddIntrinsicBigDecimal`/`JS_AddIntrinsicOperators`/`JS_EnableBignumExt` as empty no-op stubs in quickjs.c (declarations stay in quickjs.h) for ABI stability per user decision #3. Removed fork's `JSNumericOperations`/`rt->bigint_ops` indirection layer (only one slot remained — `compare`); replaced with direct `js_compare_bigint` calls in `js_relational_slow` and `js_eq_slow`. Removed `is_math_mode` dead-code stub and its 5 always-false call sites. Replaced tests/oldtests/test_bignum.js with upstream's test_bigint.js verbatim; deleted test_qjscalc.js / test_op_overloading.js; collapsed Symbol.typeofValue test's bignum-specific subtest into the main allowed-values test (bigint stays, bigfloat / bigdecimal go); microbench.js: dropped bigfloat_arith / float256_arith, added bigint32_arith. quickjs.d.ts: deleted BigFloat / BigDecimal / BigFloatEnv / BigFloatRoundingMode / Operators / OperatorSet / SelfOperators / LeftOperators / RightOperators / OperatorFunctions types and `declare var` declarations (764 → 147 lines). Regenerated all 4 bytecode fixtures (qjsbootstrap-bytecode hardcoded buffer + file-to-bytecode + libbytecode 3 inline snapshots) for the BC_VERSION + atom shifts. Audit confirms upstream's BigInt section is byte-identical to fork's (after adapting throw signatures and editorconfig whitespace stripping); the only fork-only divergence is `JS_ATOM_bigint:` in the `Symbol.typeofValue` allowed-values switch (a fork-specific extension with no upstream counterpart). test262 baseline unchanged (37/40125 errors, zero delta). vitest: 388/388 pass. test-platforms (native + win32 + wasip2) all build cleanly. |
| 543897a | PORT | added missing variable | Added `tab` to the `var` declaration in test_for_in2 in tests/oldtests/test_loop.js (was leaking to global). Trivial 1-line port of upstream's tests/test_loop.js fix. |
| ee4cd4d | PORT | compilation fix | JSBigIntBuf's first field changed from `JSBigInt big_int` to `js_limb_t big_int_buf[sizeof(JSBigInt) / sizeof(js_limb_t)]`; the two callers (js_bigint_set_si, js_bigint_from_float64) updated to cast `(JSBigInt *)buf.big_int_buf`. Avoids embedding a flexible-array-member-bearing struct as a non-final field (UB / -Wgnu-flexible-array-union-member on some compilers). |
| e9c69f7 | SKIP-NA | Fix multiarch CI builds | Touches upstream's `.github/workflows/ci.yml`, which the fork doesn't vendor (fork uses its own `ci_tests.yml`). |
| 22dbf49 | SKIP-NA | Merge pull request #391 from nickva/try-to-fix-multiplatform-builds | Upstream's PR-merge of e9c69f7 (already SKIP-NA above); no additional content. |
| 96e7965 | SKIP-NA | removed the ability to do simultaneous 64 and 32 bit x86 builds in order to simplify the Makefile | Makefile only; fork uses the Ninja/meta build and doesn't vendor Makefile. |
| 6de8885 | PORT | more bignum cleanup | quickjs.h: compacted negative-tag enum (JS_TAG_FIRST -11→-9, dropped JS_TAG_BIG_DECIMAL/JS_TAG_BIG_FLOAT, JS_TAG_BIG_INT moves -10→-9), removed declarations of JS_AddIntrinsicBigFloat / JS_AddIntrinsicBigDecimal / JS_AddIntrinsicOperators / JS_EnableBignumExt. quickjs.c: removed JS_MODE_MATH define + comment + JS_CallInternal MATH check + dead bigfloat/BigFloat-and-BigDecimal comments, AND removed the no-op stub bodies for the four removed intrinsic functions (per user-reversed decision #3 — only kept stubs because we thought upstream was; now upstream isn't, neither do we). repl.js: removed config_numcalc / has_bignum / has_jscalc / eval_mode / BigFloatEnv / bigfloat_to_string / qjscalc / numcalc paths; collapsed the styles-and-ps1 if/else to the always-taken non-numcalc branch; print() simplified to a single inspect() call; help() / cmd_start() / handle_cmd / eval_and_print_start de-bignumed. Skipped: Makefile / doc/jsbignum.texi / doc/quickjs.texi / fuzz/* / tests/test_bjson.js (fork doesn't vendor any of those). |
| 5a16c0c | PORT | fixed JS_DumpValue() for BigInt | Replaced the `#if 0`'d libbf-based JS_TAG_BIG_INT case in JS_DumpValueShort with upstream's allocation-free limb dumper. Also added the new JS_TAG_SHORT_BIG_INT case (printed as `<int64>n`). |
| 6d6893b | PORT | fixed BigInt hashing - removed -fno-bigint in qjsc and JS_AddIntrinsicBigInt() (BigInt is now considered as a base object) | (1) map_hash_key now hashes JS_TAG_SHORT_BIG_INT (via js_bigint_set_short into a JSBigIntBuf) and JS_TAG_BIG_INT (limb walk × 263 then × 3163) instead of returning 0; also fixed the silent double-XOR `return h ^= JS_TAG_FLOAT64` → `return h ^ JS_TAG_FLOAT64`. (2) JS_AddIntrinsicBigInt is now `static` and is called from JS_AddIntrinsicBaseObjects, not JS_NewContext or external callers; declaration removed from quickjs.h. (3) Removed JS_AddIntrinsicBigInt(ctx) calls from fork-only stack-limit-test/main.c, qjsbootstrap.c, quickjs-run.c (now redundant — JS_AddIntrinsicBaseObjects handles it). (4) Per user direction, dropped the now-inert `bigint: boolean` option from the `quickjs:context` Context constructor — removed the option-read and the conditional install in quickjs-context.c, removed the `bigint` field from quickjs-context.d.ts, and deleted the `bigint: false disables BigInt` test from libcontext.test.ts. (5) Updated globals.test.ts and libcontext.test.ts snapshots to reflect BigInt now being installed during base-objects setup (between globalThis and Date) instead of after Promise. (6) qjsc.c: dropped `{ "bigint", "BigInt" }` feature_list entry. (7) Removed stale `XXX: bignum` comment in JS_AtomIsNumericIndex1. (8) Ported tests/oldtests/test_builtin.js test_map split (extracted test_map1 with key_type "object"/"small_bigint"/"bigint"). |
| 7399069 | SKIP-NA | fixed examples/hello_module compilation (#240) | Makefile only; fork uses Ninja/meta build and doesn't vendor Makefile or examples/. |
| 131408f | PORT | simplified js_bigint_from_float64() | Added js_bigint_set_si64 helper that places an int64 into a JSBigIntBuf (handles 32-bit-limb case by splitting into 2 limbs); rewrote js_bigint_from_float64 to negate `mant` early and call js_bigint_set_si64 + js_bigint_shl directly, dropping the explicit js_bigint_neg+free dance. Also added `r->header.ref_count = 0` "fail safe" init in js_bigint_set_si and the new helper. |
| 9f1864a | SKIP-DONE | msan fix (#389) | Fork's js_parse_destructuring_element already initializes `label_lvalue = -1; depth_lvalue = 0;` in the destructuring var branch (src/quickjs/quickjs.c:23924-23927); upstream's 1-line add of `label_lvalue = -1` here is already present. |
| 9936606 | PORT | added new dtoa library to print and parse float64 numbers. It is necessary to fix corner cases (e.g. radix != 10) and to have correct behavior regardless of the libc implementation. | Created src/lib/dtoa/ (dtoa.c verbatim from upstream, dtoa.h verbatim, plus a dtoa.ninja.js mirroring cutils/libregexp). Wired `intermediate/dtoa.{host,target}.o` into src/archives/core.ninja.js. Removed `<setjmp.h>` from dtoa.c — never used and breaks the wasi-sdk build (wasi-libc requires `-mllvm -wasm-enable-sjlj` to expose setjmp.h). Applied the quickjs.c rewiring: added `#include "dtoa.h"`; renamed local `u64toa` → `js_u64toa` and removed the local `i64toa` (callers now use the new dtoa.h `i64toa_radix`/`i32toa`); removed `js_strtod` (callers use `js_atod` with JS_ATOD_INT_ONLY flag); collapsed the 233-line js_ecvt1/js_ecvt/js_fcvt1/js_fcvt/js_dtoa1/js_dtoa block plus the 73-line js_dtoa_radix into a single 22-line js_dtoa2 wrapper that calls js_dtoa with a stack buffer (or malloc'd buffer if too long) and js_new_string8s the result. Updated all call sites: JS_ToStringInternal (INT case via i32toa, FLOAT64 via js_dtoa2 with JS_DTOA_FORMAT_FREE), js_number_toString (INT via i64toa_radix; FLOAT via js_dtoa2 with optional JS_DTOA_EXP_DISABLED for non-decimal radix), js_number_toFixed (JS_DTOA_FORMAT_FRAC unless |d|>=1e21), js_number_toExponential (JS_DTOA_EXP_ENABLED), js_number_toPrecision (JS_DTOA_FORMAT_FIXED). js_bigint_to_string1 likewise switched to i64toa_radix + js_new_string8 for the short-bigint case and tracks buf_end to compute the final string length. Added the upstream test_number additions (toPrecision(1)→"3e+1", toString(7), toString(35)) to tests/oldtests/test_builtin.js. SKIP-NA: Makefile change, doc/jsbignum.texi etc. don't apply (fork doesn't vendor them). |
| 978756a | PORT | protect against printf errors (#319) | dbuf_printf in src/lib/cutils/cutils.c: bail with `return -1` when `vsnprintf` returns negative (encoding error), rather than treating it as a length and reading past the buffer. |
| 9f65ef1 | PORT | simplified and fixed backtrace_barrier (#306) | Refactored backtrace barrier from a per-bytecode flag to a per-stack-frame js_mode bit. Added `JS_MODE_BACKTRACE_BARRIER (1 << 3)`. JS_EvalInternal now sets that bit on the calling stack frame (saving and restoring) instead of stamping it onto the JSFunctionDef/JSFunctionBytecode. build_backtrace breaks the walk when it sees the bit, replacing the BC-stored `backtrace_barrier` field. Removed: `backtrace_barrier` from JSFunctionBytecode (frees up another flag bit; comment updated `4 bits` → `5 bits`), `backtrace_barrier` from JSFunctionDef, the bc_set/get for it in JS_WriteFunctionTag/JS_ReadFunctionTag, and the JS_BACKTRACE_FLAG_SINGLE_LEVEL flag (callers in js_parse_error and js_parse_postfix_expr regex path now pass 0). Fork-specific build_backtrace state (first_frame_filename/synthetic frames) is preserved verbatim. **Bytecode flag-bit position shift: this is technically a bytecode format change, but BC_BASE_VERSION isn't bumped (matches upstream's choice).** Tests: 387/387 vitest pass; test262 baseline unchanged. |
| 4941398 | PORT | fixed hash_map_resize() - added Map/WeakMap in microbench | map_hash_resize: dropped the `new_hash_size += slack / sizeof(*new_hash_table)` line — it expanded the hash size after the realloc but not all subsequent code accounted for the larger size. (1-line bugfix.) microbench.js: replaced set_collection_add with the four upstream variants (map_set / map_delete / weak_map_set / weak_map_delete), preserving fork's prettier-style multi-line var declarations. |
| d1bb520 | PORT | reduced memory usage of Map hash table | Replaced JSMapRecord's `struct list_head hash_link` with a pointer `JSMapRecord *hash_next` (singly-linked chain), and JSMapState's `struct list_head *hash_table` with `JSMapRecord **hash_table`. js_map_constructor uses js_mallocz to zero the table; map_hash_resize memsets it after realloc. Direct singly-linked-list operations (mr->hash_next = head; head = mr) replace list_add_tail. js_map_delete and reset_weak_ref now walk the chain to find-and-unlink before calling map_delete_record (which no longer touches the hash table — added a comment to that effect). map_hash_key dropped its unused `ctx` param. js_map_clear memsets the hash table before iterating and freeing records. ~1 pointer per bucket saved compared to the doubly-linked list. |
| a44011e | PORT | enable dtoa tests on win32 | tests/oldtests/test_builtin.js: removed the early-return win32/cygwin guard in test_number now that the new dtoa library produces deterministic output across platforms; added the upstream additions ((1-2**-53).toString(12), (10^18+128) .toString/.toFixed, (±0.5).toFixed(0)). |
| 372ad84 | PORT | more dtoa bench (Charlie Gordon) | meta/microbench.js: rewrote int_to_string and float_to_string to exercise three magnitudes per iteration, added int_toString / float_toString / float_toFixed / float_toPrecision / float_toExponential variants, and registered them in main()'s test list. (Skipped upstream's accidental duplicate `function int_to_string(n)` — that's an upstream bug where the second definition shadows the first; kept just one merged definition.) |
| 156d981 | PORT | added string ropes for faster concatenation of long strings (issue #67) | Big patch (~600 lines in quickjs.c). git apply --3way handled most of it; one fork-adapt was needed: a new `JS_ThrowInternalError(ctx, "string too long")` callsite needed the fork's `(ctx, "<internal>/quickjs.c", __LINE__, ...)` shape. Also moved `typedef struct JSObject JSObject` and the JS_VALUE_GET_OBJ/STRING macros out of quickjs.h into quickjs.c (matching upstream); added new `JS_TAG_STRING_ROPE = -6` tag and `JSStringRope` struct; new JS_VALUE_GET_STRING_ROPE macro; rope-aware variants for hashing, comparison, indexing, iteration etc. tests/oldtests/test_builtin.js gets the new test_rope (rope_concat × 2 directions, 100k chars). meta/microbench.js gets string_build_large1 (append) and string_build_large2 (prepend) benches. test262 baseline unchanged; vitest 387/387 pass; build clean across native + win32 + wasip2. |
| 6cc02b4 | PORT | more use of js_new_string8 - inlined JS_NewString() (initial patch by Charlie Gordon) | git apply --3way produced two conflicts. (1) quickjs.h: keep upstream's `static inline JSValue JS_NewString(...)` definition that delegates to JS_NewStringLen; drop the fork's previous extern declaration. Added `#include <string.h>` to quickjs.h since the inline now calls `strlen()`. (2) quickjs.c js_string_charAt out-of-range branch: fork doesn't merge js_string_charAt and js_string_at the way upstream does, so kept fork's separate functions but switched the empty-string return from `js_new_string8(ctx, NULL, 0)` to `JS_AtomToString(ctx, JS_ATOM_empty_string)` (matches upstream's non-`is_at` branch and works with the renamed `js_new_string8` 2-arg signature). Other `js_new_string8(ctx, "...")` calls were already 2-arg form so no further adapt needed. Required a clean rebuild (meta/clean.sh) — turning JS_NewString into a static inline made stale .o files reference an extern that no longer exists. |
| 2a4f629 | SKIP-NA | added -Wno-infinite-recursion | Makefile only; fork's Ninja CFLAGS don't set any -Wno-* (the warning isn't surfacing in the fork's build path). |
| 29630bc | SKIP-DONE | added missing header | `#include <string.h>` already added to quickjs.h in the 6cc02b4 port (ours covered both upstream commits' diffs together). |
| b31bb20 | PORT | updated to unicode 16.0.0 (bnoordhuis) - updated test262 | Full update per user direction. Bumped test262 submodule from 3eaa7c93 (Unicode 15) to 56e77d63 (Unicode 16, "Update link to test262-harness repo"). Bumped unicode_download.sh URL 15.0.0 → 16.0.0 and re-downloaded all 12 UCD files (CaseFolding/DerivedNormalizationProps/PropList/SpecialCasing/CompositionExclusions/ScriptExtensions/UnicodeData/DerivedCoreProperties/NormalizationTest/Scripts/PropertyValueAliases/emoji-data) — the build regenerates libunicode-table.h from these. Applied unicode_gen.c changes: parse_derived_core_properties now stops at `;` so Unicode 16's new InCB property values parse correctly; added two new RUN_TYPE_UF_EXT2/UF_EXT3 cases (u_len=2, l_len=0, f_len=1 for U+FB05 LATIN SMALL LIGATURE LONG S T, and u_len=3, l_len=0, f_len=1 for U+1FD3/U+1FE3 GREEK letters). Applied unicode_gen_def.h changes: 7 new scripts (Garay, Gurung_Khema, Kirat_Rai, Ol_Onal, Sunuwar, Todhri, Tulu_Tigalari) + 5 new properties (IDS_Unary_Operator, Modifier_Combining_Mark, ID_Compat_Math_Start, ID_Compat_Math_Continue, InCB). Applied test262.conf changes: dropped Array.prototype.flatten / cleanupSome / FinalizationGroup / FinalizationRegistry.prototype.cleanupSome, added Atomics.pause / canonical-tz / Error.isError / explicit-resource-management / Float16Array / import-defer / 14 Intl.* / iterator-sequencing / Math.sumPrecise / promise-try / regexp-modifiers / RegExp.escape / source-phase-imports{-module-source} / uint8array-base64 (all =skip), and added the test262/test/staging/ exclude. Updated tests/test262.patch's regExpUtils.js index hash for the test262 commit's content. SKIP-NA: TODO file (fork doesn't track). test262_errors.txt baseline jumped from 37 → 93 errors due to new test262 tests exercising pre-existing fork limitations (annexB HTML-close comments, AsyncFromSyncIteratorPrototype $DONE not called, Date setX value-read-before-ToNumber spec, Function.prototype.arguments/caller same-getter-setter, RegExp v-flag CharacterClassEscapes, String custom-matcher-on-primitive). Fork's test count rose from ~36k to 40260 (test262 added tests). |
| dec4aca | SKIP-DONE | update test262_errors.txt | Fork's test262_errors.txt was already updated as part of the b31bb20 port commit (the test262 submodule bump triggered the new errors). Fork's baseline differs from upstream's anyway because the fork's engine has fork-specific divergences from spec. |
| d20ffec | PORT | exit by default on unhandled promise rejections (issue #305) | Three pieces beyond the upstream verbatim: (1) qjs.c default flipped — `--unhandled-rejection` flag becomes `--no-unhandled-rejection`, default-on. (2) QJU_PrintPromiseRejection in quickjs-utils.c gains `exit(1)` after dumping. (3) Per user direction, propagated the tracker install to the fork's other JS-running binaries (quickjs-run, qjsbootstrap, stack-limit-test). The naive port broke many vitest snapshots because the fork's sync-module path translates rejections via JS_Throw, which doesn't mark the underlying module promise as handled — the tracker would fire even though the user's try/catch caught the resulting exception. Fixed in the engine: (a) refactored js_async_function_call into js_async_function_call_internal with a `mark_handled` flag, and js_execute_sync_module now passes TRUE so its consumed-by-the-caller promise is marked handled before resume; (b) js_evaluate_module_async gained a `mark_handled` parameter (TRUE only for the sync wrapper) that marks m->promise handled right after JS_NewPromiseCapability; (c) **fulfill_or_reject_promise now defers the rejection-tracker call to a microtask** via JS_EnqueueJob, with the job re-checking `s->is_handled` before reporting — this gives same-tick `.then` / `await` / `catch` time to attach (matching browser/Node semantics), so `try { await Promise.reject(...) } catch` patterns work cleanly under the new exit-on-unhandled regime. Added a small static helper js_promise_set_handled near JSPromiseData. test_std.js fixed to use engine.gc instead of std.gc (latent fork-incompat surfaced now that unhandled rejections exit). |
| e8cfe8f | PORT | removed memory leak in string padding (issue #274) | One-line fix in js_string_pad: when n > JS_STRING_LEN_MAX after argv[1] was already converted to v, the failure goto jumped to fail2 (frees only str), leaking v. Changed to fail3 (frees v and str). |
| d045a13 | PORT | disable rejection tracker in the repl - repl cleanup | qjs.c: added `JS_SetHostPromiseRejectionTracker(rt, NULL, NULL);` immediately before the `QJMS_EvalBinary(... qjsc_repl ...)` call inside the `if (interactive)` block, so REPL typos that produce unhandled rejections no longer reach the host tracker. Critical in the fork because QJU_PrintPromiseRejection now `exit(1)`s (per d20ffec port) — without this, every REPL syntax slip would kill the process. repl.js: dropped the `is_async` parameter from `eval_and_print_start`; always use `async: true` and `.then(print_eval_result, print_eval_error)`. |
| b0c1a12 | PORT | fixed set_date_field() | The fork had previously rewritten set_date_field (in 6b87abb's port of upstream 65ecb0b) using a separate `any_nonfinite` flag instead of clobbering `res`, but the rewrite still ended with an unconditional `JS_SetThisTimeValue(this, d)` — which clobbered the [[DateValue]] mutation that an argument's valueOf may have made during the ToNumber loop, when thisTimeValue was originally NaN. Per spec step 8 ("If t is NaN, return NaN") + the test262 case (date-value-read-before-tonumber-when-date-is-invalid), valueOf's side effect on `this` must be preserved. Fix: when `res==0` after the ToNumber loop, `return JS_NAN` directly without calling SetThisTimeValue. Removed 12 entries from test262_errors.txt; baseline now 81/40260 (was 93/40260). |
| 67de495 | PORT | fixed typed array set operation when obj != receiver | Verbatim port of upstream's `if (p == p1)` guard around the typed-array OOB ToNumber coercion in `JS_SetPropertyInternal` (typed_array_oob label). When obj != receiver (i.e. setting a typed-array index via the prototype chain or Reflect.set with a non-typed-array receiver), the spec requires NOT coercing the value — only when the receiver is the typed array itself. The `else` branch frees val without coercion. Removes 6 entries from test262 baseline (TypedArrayConstructors/internals/Set: prototype-chain-set, reflect-set, receiver-is-not-object, receiver-is-not-typed-array). Baseline now 75/40260. |
| 56c47f7 | PORT | fixed exception handling in AsyncFromSyncIterator and async for of | quickjs-opcode.h: added OP_for_await_of_next (1 byte, pops/pushes 3→4) and changed OP_iterator_get_value_done arity (1→2 stack inputs, 2→3 outputs) — the new opcode pre-installs JS_UNDEFINED into the catch_offset slot before the .next() call so an exception there doesn't trigger iterator_close, and iterator_get_value_done re-arms the catch offset after extracting value/done. js_parse_for_in_of: replaced the inline `dup3 / drop / call_method` sequence with a single OP_for_await_of_next emit. Added js_async_from_sync_iterator_close_wrap[_func_create] helpers (rejection handler that re-throws and closes the sync iter). Rewrote js_async_from_sync_iterator_next: throw-not-method case now closes the sync iter and throws TypeError instead of forwarding the user's value as the rejection reason; the reject/done_resolve labels moved into the value_wrapper_promise-exception block where they additionally close the sync iter on next/throw paths; perform_promise_then now installs the close_wrap handler as resolve_reject[1] for non-RETURN, non-done flows so a rejection from the value wrapper closes the sync iter. Test fixture updates (no logic): regenerated the hardcoded `bytecode` Buffer in tests/qjsbootstrap-bytecode.test.ts and updated 3 inline bytecode-hex snapshots in tests/file-to-bytecode.test.ts and tests/libbytecode.test.ts (opcode IDs shifted by 1 because of the new opcode insertion before iterator_get_value_done). Removed 12 entries from test262 baseline (AsyncFromSyncIteratorPrototype next/throw cases). Baseline now 63/40260. |
| 2634856 | PORT | removed invalid tests | test262.conf only — verbatim port of upstream's exclusion list (12 RegExp/CharacterClassEscapes tests under "feature regexp-v-flag is missing in the tests" + 23 String/prototype/{match,matchAll,replace,replaceAll,search,split}/cstm-*-on-{bigint,boolean,number,string}-primitive tests under "not yet in official specification"). De-duped: upstream's diff lists each path twice (artifact). SKIP-NA: TODO file (fork doesn't track). Resolves 35 baseline entries — baseline now 28/40225. |
| 0d7aaed | PORT | ensure that JS_IteratorNext() returns JS_UNDEFINED when done = TRUE (#394) | Three pieces, verbatim from upstream: (1) JS_IteratorNext now branches done==0 / done==1 / done==2 explicitly — when done==1 (i.e. JS_IteratorNext2 returned a value but done is true), free the obj and return JS_UNDEFINED instead of leaking the obj as the value. (2) Three callers (js_object_fromEntries, js_map_constructor, js_array_from_iterator) drop their now-redundant `JS_FreeValue(ctx, item); break;` for `break;` since the function already returns JS_UNDEFINED on done. Test262 baseline unchanged (28/40225) — these tests must already be exercising the value-on-done path either way. |
| 6ac04e1 | PORT | removed useless printf() (#257) | One-line: removed the `printf("__JS_FreeValue: unknown tag=%d\\n", tag)` immediately before `abort()` in __JS_FreeValueRT — the abort already produces a debugger-visible signal and the printf only confused stdout-redirected callers. |
| bf164d6 | PORT | fixed eval with empty argument scope (#249) | Verbatim port. Reduced JS_MAX_LOCAL_VARS from 65535 to 65534 with new comment noting that (-1) and ARG_SCOPE_END are reserved sentinel values. Switched the encoding/decoding offsets in OP_eval / OP_apply_eval handling: `- 1` / `+ 1` → `+ ARG_SCOPE_END` / `- ARG_SCOPE_END` (= -2 / +2) — this lets the empty arg scope be encoded distinguishably from the global scope. Test262 baseline unchanged. |
| 8b5b127 | PORT | reworked weak references so that cycles are (hopefully) correctly handled - added Symbol as WeakMap key, WeakRef and FinalizationRegistry | Large port — 600 lines of GC code. Ported via `git apply --3way`, resolved 4 conflicts manually: (1) class-id enum landed in upstream's quickjs.c but lives in the fork's quickjs.h — added JS_CLASS_WEAK_REF / JS_CLASS_FINALIZATION_REGISTRY there. (2) JS_FreeRuntime: kept the fork-specific "Run GC twice" workaround for Context objects, but using the new JS_RunGCInternal(rt, FALSE). (3,4) Dropped fork's old `delete_weak_ref` and `reset_weak_ref` bodies — fully replaced by upstream's `js_weakref_free` / `map_delete_weakrefs`. Also adapted upstream's `JS_ThrowTypeError(ctx, "...")` calls to the fork's signature `JS_ThrowTypeError(ctx, "<internal>/quickjs.c", __LINE__, "...")` — 7 sites in WeakRef/FinalizationRegistry constructors and FinalizationRegistry.register/unregister. quickjs-atom.h: added WeakRef + FinalizationRegistry atoms. quickjs.h: added JS_AddIntrinsicWeakRef declaration + 2 class-id enum entries. test262.conf: flipped FinalizationRegistry, symbols-as-weakmap-keys, WeakRef from `=skip` to enabled (98 new tests now run). tests/oldtests/test_builtin.js: ported test_weak_map's Symbol-key cases, added test_weak_ref + test_finalization_registry — used `require("quickjs:engine").gc()` (fork's std.gc was removed; test_builtin.js is a script, can't use ESM imports). Test fixture updates: regenerated qjsbootstrap-bytecode buffer (3 byte shifts from new class IDs); updated 4 inline bytecode-hex snapshots and the globals.test.ts snapshot (WeakRef/FinalizationRegistry now appear as global functions). Test262 baseline 28/40323 — same error count as before (was 28/40225); the 98 new tests from enabled features all pass cleanly. |
| b342502 | PORT | avoid freeing an object structure in js_weakref_free() if it is about to be freed in free_zero_refcount() | Verbatim port. Three small changes preventing a use-after-free in the new weakref logic: (1) free_object: when keeping a dummy struct alive (weakref_count > 0), reset header.mark = 0 so a later js_weakref_free can claim ownership of the free. (2) __JS_FreeValueRT: when queuing an object onto gc_zero_ref_count_list, set mark = 1 to claim it — js_weakref_free must NOT double-free. (3) js_weakref_free: now checks `mark == 0` in addition to ref_count==0 / weakref_count==0 before freeing. Added test_weak_map_cycles to test_builtin.js (uses require("quickjs:engine").gc() pattern). Test262 baseline unchanged. |
| f121cbd | PORT | added forgotten js_weakref_is_live() tests | Verbatim port. js_weakref_deref now checks js_weakref_is_live before returning the target — fixes a crash where `new WeakRef({}).deref()` returned a zombie pointer to an already-freed dummy structure. js_finrec_unregister gained an is_live check on fre->token before js_same_value (avoids comparing a dead token's pointer to a live one). Added forgotten test_weak_ref deref-without-gc assertions to test_builtin.js. Test262 baseline unchanged. |
| beeb272 | PORT | 'undefined' is a valid let/const variable name. It gives a SyntaxError at top level because it is already defined (#370) | One-line fix: removed the `name == JS_ATOM_undefined` check from the lexical-name validator in js_define_var. `undefined` is reserved as a global identifier, not as an AssignmentTarget, so `let undefined = 1` is well-formed grammar — the SyntaxError comes later at top-level scope binding because the global is already defined. Test262 baseline unchanged. |
| c1bf4e9 | PORT | workaround for overflow test in JS_GetOwnPropertyNamesInternal() (#111) | Verbatim port. Replaces the unguarded `atom_count = num_keys_count + str_keys_count + sym_keys_count + exotic_keys_count` (which silently wraps on overflow and then mallocs a tiny buffer) with stepwise additions and per-step wrap detection — falls into a single `add_overflow:` label that throws OOM and frees tab_exotic. Test262 baseline unchanged. (Two unrelated wine-test flakes during verification — different tests each run, both passed on re-execution; not caused by this change.) |
| 159fe28 | PORT | fixed module cyclic imports (#329) | Engine port verbatim. Three pieces: (1) JS_AutoInitProperty: when the autoinit returns a value tagged JS_TAG_STRING from a JS_AUTOINIT_ID_MODULE_NS handler, treat it as a smuggled JSVarRef* (set JS_PROP_VARREF, increment ref_count) — used by the cyclic-import resolver to delegate to a varref instead of a namespace. (2) js_module_ns_autoinit rewritten: was always returning the ns, now resolves the export via js_resolve_export and returns either a real namespace (`* as` re-exports) or a varref-encoded-as-string. (3) ExportedNameEntryEnum renamed EXPORTED_NAME_NS → EXPORTED_NAME_DELAYED; the union loses its `module` field; js_build_module_ns now categorises both ns-re-exports AND null-var_ref entries as DELAYED so resolution happens lazily through the autoinit. SKIP-NA: tests/{assert.js, fixture_cyclic_import.js, test_cyclic_import.js} — the upstream tests use test262-style /*--- negative ---*/ frontmatter that the fork's vitest infrastructure doesn't process. SKIP-NA: Makefile (fork uses Ninja). Test262 baseline unchanged. (One unrelated wine-test flake during verification.) |
| 00b709d | PORT | flush stdout in console.log() (#309) | Adapted to fork's structure: upstream wraps a single console.log binding with fflush; the fork has one shared js_print used by `print` and console.{log,info,warn,error}. After confirming with user, fflush is added at the bottom of js_print itself — flushes in `print` too. tests/os-exec.test.ts: snapshot updated for the resulting output-ordering change in "os.exec - run a command with block: false and waitpid" — parent's `print` no longer buffers behind the forked `echo` child, so "got pid:" now prints before "hello async". Test262 baseline unchanged. |
| 1943101 | SKIP-NA | updated Changelog | Changelog only — fork doesn't track upstream's Changelog file (per protocol). |
| c805d4f | PORT | fixed weakmap gc (#398) | Verbatim port. map_delete_weakrefs now removes the dead-key entry from the hash table BEFORE calling map_delete_record — previously map_delete_record would re-hash the dead key (whose pointer storage may have been freed in the meantime), tripping over a stale pointer. The new code walks the hash chain and unlinks first, with a `goto done` for the case where a hash resize already removed the entry. Test262 baseline unchanged. |
| ec83bd2 | PORT | qjs: allow SI suffixes in memory sizes - set default stack size to 1 MB | Verbatim port. New get_suffixed_size helper in qjs.c (G/M/k/K) replaces direct strtod for `--memory-limit` and `--stack-size`; help text updated. quickjs.h: JS_DEFAULT_STACK_SIZE bumped from 256K to 1M, wrapped in `#ifndef` so embedders can override. Test262 baseline unchanged. |
| fa706d5 | PORT | Fix leak in BigInt unary plus (saghul) | One-line fix: added `JS_FreeValue(ctx, op1)` before `goto exception` at the `JS_TAG_BIG_INT` OP_plus throw site in js_unary_arith_slow — op1 is heap-allocated and was leaking when unary `+` threw on a bigint. Did NOT add the free at the parallel `JS_TAG_SHORT_BIG_INT` site — both fork and current upstream have the inline/heap split (introduced by upstream 61e8b94's bigint rewrite), and upstream's fa706d5 deliberately fixed only the heap site since op1 is inline-tagged on the SHORT path. Test262 baseline unchanged. |
| 083b7ba | PORT | Fix UB in BigInt left shift (saghul) | One-character fix in js_binary_logic_slow's `bigint_shl` block: cast to unsigned `js_dlimb_t` (was `js_sdlimb_t`) before shifting — left-shifting a negative signed value is C UB. Test262 baseline unchanged. |
| 2b6cf57 | PORT | removed unused slack in hash_map_resize() (saghul) | Verbatim cleanup: `js_realloc2(... &slack)` → `js_realloc(...)` and `size_t slack;` removed from map_hash_resize. The slack output was never read. Test262 baseline unchanged. |
| f05760c | PORT | qjs: added performance.now() | Adapted to fork's per-module global-injection pattern (per user direction): added `js_os_add_performance_global(ctx)` in src/builtin-modules/quickjs-os/quickjs-os.c (and declared in quickjs-os.h) that creates `globalThis.performance = { now: js_os_now }`. Wired into quickjs_full_init_globals after js_timers_add_globals. Added a forward decl for js_os_now since the existing definition lives ~600 lines below. globals.test.ts snapshot updated to include the new performance entry. Test262 baseline unchanged. |
| a151ce1 | PORT | fixed and improved Map/Set hashing | Verbatim port via git apply --3way; one trivial conflict at js_map_delete (line whitespace difference around the `h = map_hash_key(...)` call). New `hash_bits` field on JSMapState; new HASH_MUL32/64 multipliers + map_hash32/map_hash64/map_hash_pointer helpers (Knuth golden-ratio mixing). map_hash_key now takes hash_bits and folds the masking into the multiplier — every previous `map_hash_key(...) & (s->hash_size - 1)` site simplifies to `map_hash_key(..., s->hash_bits)`. Bigint hashing gets a final map_hash32 mix to avoid h-mod-n only depending on p->tab[i] mod n. SKIP-NA: tests/microbench.js (fork doesn't run upstream's microbench). Test262 baseline unchanged. |
| 1eb05e4 | PORT | fixed buffer overflow in BJSON String and BigInt reader (#399) | Two fixes in JS_ReadString / JS_ReadBigInt: (1) JS_ReadString now bounds-checks `len > JS_STRING_LEN_MAX` after the wide-char shift and throws InternalError instead of overflowing js_alloc_string. (2) JS_ReadBigInt's allocation size was rewritten as `(len - 1) / (JS_LIMB_BITS / 8) + 1` — same value when len > 0, but doesn't trip the `if (len == 0) return short_zero` early-return guarantee under integer-overflow analysis. Test262 baseline unchanged. |
| 00e6f29 | PORT | added JS_GetAnyOpaque() (oleavr) | New public C API: `JS_GetAnyOpaque(JSValueConst obj, JSClassID *class_id)` returns the opaque pointer without a class_id pre-condition, writing the actual class id back through the out-param (or 0/NULL if obj isn't an object). Used by embedders that need to distinguish polymorphic native objects. Verbatim port to quickjs.c + decl in quickjs.h. Test262 baseline unchanged. |
| 9d3776d | PORT | fixed break statement in the presence of labels (bnoordhuis) (#275) | Verbatim port. New `is_regular_stmt` bit on BlockEnv distinguishes labelled non-loop statements from loops; `emit_break` now only treats unlabelled `break` as targeting the entry when it's NOT a regular labelled statement. Set in js_parse_statement_or_decl right after push_break_entry for the labelled-regular-statement branch. push_break_entry initializes the new bit to FALSE. SKIP-NA: tests/test_language.js (fork's oldtests doesn't replicate every upstream test addition). Also bumped tests/create-process.test.ts testTimeout 15s → 60s in this commit's verification run — repeated wine-spawn timeouts during this session's suite-under-load weren't this commit's fault but the test-config tightness needed addressing per .claude/rules/no-wine-blame.md. Test262 baseline unchanged. |
| 25ffdb4 | PORT | fixed the handling of unicode identifiers | Verbatim port. New static helper count_ascii(buf, len) factors out the ASCII-prefix scan that JS_NewStringLen already had inline. JS_NewAtomLen now requires the entire input to be ASCII before going through __JS_FindAtom — previous fast path silently returned a wrong atom when the str contained non-ASCII bytes, since FindAtom hash-indexed the raw UTF-8 bytes against atoms canonicalised to UCS-2. JS_NewStringLen's inline ASCII-scan replaced by the helper call. SKIP-NA: tests/test_language.js (fork's oldtests doesn't replicate every upstream test addition). Test262 baseline unchanged. |
| c505ac0 | PORT | fixed JS_IsString() with ropes | One-line fix in the public inline JS_IsString: also returns TRUE for JS_TAG_STRING_ROPE values (which are still strings as far as the JS API is concerned). Previously rope-tagged strings would be misclassified by embedders. Test262 baseline unchanged. |
| d546fbf | PORT | changed js_throw_type_error ES5 workaround to be more compatible with test262 | Verbatim port. Function.prototype.{caller,arguments} previously had a separate `js_function_proto_caller` setup that returned undefined for non-strict caller access AND a separate `js_throw_type_error` for the setter — making get and set distinguishable functions, which test262 caught (prop-desc test expects same getter+setter function). Now js_throw_type_error itself implements the relaxation (returns undefined when called with no args from a non-strict, has-prototype function bytecode; otherwise throws), and gets installed as both getter AND setter for caller/arguments. Removes 2 entries (10 lines counting the long Test262Error messages) from baseline — now 26/40323. SKIP-NA: TODO file. |
| 949c105 | PORT | fixed class field named get or set | Verbatim parser fix in js_parse_property_name: when parsing class fields (allow_private=TRUE), if the token is `get`/`set` and the next token is `\n` (i.e. ASI to terminate the field), treat it as a field name rather than the start of a getter/setter declaration. Also added `';' && allow_private` to the ident-found follow-set so `class { get; }` parses as a field. SKIP-NA: TODO file. Removes 2 entries from baseline — now 24/40323. |
| 7adeb5c | PORT | Fix exporting destructured variables (saghul) (#382) | New `BOOL export_flag` parameter on js_parse_destructuring_element threaded through 8 call sites. When TRUE and a destructured var is defined, also calls add_export_entry — fixes `export const { a, b } = obj`. The fork's js_parse_var already takes a BOOL export_flag (matches upstream) so the call inside js_parse_var passes that through. Internal recursive calls inside js_parse_destructuring_element (object property and array element paths) also propagate export_flag. All other call sites pass FALSE. SKIP-NA: TODO file. Removes 1 entry from baseline — now 23/40323. |
| 9918c12 | PORT | workaround for #282 | Verbatim port. find_jump_target now stashes the original `label0` and resets `label = label0` on cycle detection, instead of leaving `label` at whatever the last-followed value was — avoids the dead-code skipper miscalculating jumps for cyclic labels like `for (;;) { l:break l; ... }`. Acknowledged as a workaround in the function body comment. SKIP-NA: tests/test_loop.js (fork's oldtests doesn't replicate every upstream test addition). Test262 baseline unchanged. |
| c50de13 | PORT | indent fix | One-line indent cleanup in JS_NewAtomLen left over from the 25ffdb4 unicode-identifier port — the inner `JSAtom atom = ...` declaration was over-indented. No semantics change. Skipped test262 since no behaviour change. |
| 67b48ae | PORT | - removed the 'use strip' extension - removed the JS_EVAL_FLAG_STRIP eval flag and replaced it with JS_SetStripInfo() which has simpler semantics. - qjs: added the '-s' and '--strip-source' options - qjsc: added the '-s' and '--keep-source' options | Engine port verbatim: new `JS_SetStripInfo`/`JS_GetStripInfo` C API + `JS_STRIP_SOURCE`/`JS_STRIP_DEBUG` flags on rt; per-fd `strip_debug`/`strip_source` bits set from `rt->strip_flags` in js_new_function_def; all `js_mode & JS_MODE_STRIP` sites switched to per-fd flags; bytecode write/read gain optional source storage. The `"use strip"` directive parser and `JS_EVAL_FLAG_STRIP` are gone. NOTE: upstream's diff has 3 `s->strip_debug` typos inside `#ifdef DUMP_BYTECODE` blocks where the function takes `fd`; ported as `fd->strip_debug` (only matters for debug builds). qjs.c: added `-s` (strip all debug) and `--strip-source` (strip source) CLI options; calls JS_SetStripInfo. fork's quickjs-os.c (worker code, replaces upstream's quickjs-libc.c quickjs-libc): WorkerFuncArgs gains strip_flags; worker_func calls JS_SetStripInfo on the new runtime; js_worker_ctor reads parent's strip_flags via JS_GetStripInfo. run-test262.c: dropped JS_EVAL_FLAG_STRIP from harness eval. SKIP-NA: tests/test_loop.js, doc/quickjs.texi, Makefile, repl.js (fork's repl.js never had "use strip"). qjsc.c: per user direction, replaced fork's qjsc.c with upstream's full 67b48ae rewrite (hand-rolled CLI, get_suffixed_size + get_short_optarg helpers, `-s` + `--keep-source` options, defaults to JS_STRIP_SOURCE) and re-applied the fork-specific edits on top: __wasi__ guard around <sys/wait.h>, swap quickjs-libc.h for quickjs-utils.h/quickjs-modulesys.h/debugprint.h with QJSC_MINIMAL ifdef, js_std_dump_error→QJU_PrintException, js_load_file→QJU_ReadFile, JS_NewCModule's 4th NULL arg, JS_ThrowReferenceError fork signature, debugprint instrumentation throughout, JS_SetModuleNormalizeFunc/JS_SetModuleLoaderFunc/JS_SetModuleLoaderOpaque split call, fork's larger system-modules list (quickjs:std/timers/os/cmdline/+QJSC_MINIMAL-gated bytecode/context/engine/encoding), main_c_template1/2 rewritten to use quickjs_full_init/QJMS_*/js_eventloop_*/js_print_*/js_cmdline_add_scriptArgs/QJU_PrintException, js_std_eval_binary→QJMS_EvalBinary with NULL filename. Ran a full clean rebuild (167/167 ninja steps including all qjsc invocations: repl.js, sample-program/sum, etc.) before retesting — qjsc handles the build-system needs unchanged. tests/qjsbootstrap-bytecode.test.ts buffer regenerated for the new bytecode source-len field; 4 inline bytecode-hex snapshots in tests/file-to-bytecode.test.ts and tests/libbytecode.test.ts re-recorded. Test262 baseline unchanged. |
| 5b0c98a | PORT | fixed HTML comments (chqrlie) | New `buf_start` field on JSParseState (initialized in js_parse_init alongside buf_ptr). The `-->` HTML close-comment recognizer now triggers when either there's a leading line-feed (`s->got_lf`) OR the current token is the very first one in the buffer (`s->last_ptr == s->buf_start`) — previously it relied on `last_line_num != line_num` which mis-fires on first-line `-->` cases. SKIP-NA: TODO file. Resolves 3 baseline entries (annexB single-line-html-close-first-line-{1,2,3}). Now 20/40323. |
| 9106fa0 | SKIP-DONE | fixed DUMP_BYTECODE | This is the fix for the 3 `s->strip_debug` typos in js_create_function's DUMP_BYTECODE blocks. The fork already uses `fd->strip_debug` at all three sites — corrected during the 67b48ae port (since `s` isn't in scope in js_create_function, which takes `fd` directly; the upstream typos only "worked" in DUMP_BYTECODE-disabled builds). Other s->strip_debug refs in the codebase (compute_pc2line_info, resolve_labels) are correct because there `s` is the JSFunctionDef parameter. |
| 4cc4c6c | PORT | optimized js_parse_class_default_ctor() (bnoordhuis) | New OP_init_ctor opcode emits the equivalent of `super(...arguments)` directly instead of re-parsing a string. js_parse_class_default_ctor rewritten to construct the JSFunctionDef directly + emit bytecode; previously it temporarily redirected the parser's buf_ptr/buf_end at a literal `"(){super(...arguments);}"`/`"(){}"` source, ran js_parse_function_decl2, then restored. Forward declarations for emit_class_field_init, js_new_function_def, emit_return added in front of the new function; the old emit_return forward decl later in the file removed (now redundant). OP_check_ctor's body gains a `non_ctor_call:` label that OP_init_ctor's "called without new" path goto's to. Test fixtures: regenerated qjsbootstrap-bytecode buffer + 4 inline bytecode-hex snapshots for the new opcode shift. Test262 baseline unchanged. |
| 251a8b2 | PORT | added column number in error messages - simplified parser | Parser-simplification + per-instruction column tracking. JSFunctionDef/JSToken/JSParseState/JSParsePos drop their `line_num`/`last_line_num` fields; positions are now tracked as raw `const uint8_t *` source pointers and converted to line/col on demand via new `get_line_col`/`get_line_col_cached` helpers + a `GetLineColCache` carried on JSParseState. `find_line_num` gains `int *pcol_num` out-param; pc2line bytecode format gains a leading line+col leb128 pair plus per-instruction column deltas (read by `find_line_num`/`dump_pc2line`, written by `compute_pc2line_info`). `b->debug.line_num` removed; JSFunctionBytecode's `JS_CGETSET_DEF("lineNumber", ...)` becomes a magic getter shared with new `columnNumber` getter that derives both via `find_line_num(b, -1, &col)`. `JSAtom JS_ATOM_columnNumber` added. New `emit_source_pos`/`has_lf_in_range` helpers; `emit_op` no longer auto-emits OP_line_num — callers in postfix/binary/return/throw/hasexpr paths invoke `emit_source_pos` explicitly. JS_PushSyntheticStackFrame gains a `col_num` parameter (callers updated to pass 0). build_backtrace gains `col_num` param; the fork's `first_frame_*` capture mechanism extended to also write `JS_ATOM_columnNumber`. Synthetic frames now carry an internal `deferred_pop` flag: `JS_ThrowErrorWithProto3`'s old eager pseudo-frame is replaced by a deferred-pop synthetic frame that survives until JS_CallInternal's `exception:` runs `build_backtrace` (with sf->cur_pc=after-failing-op, matching upstream), then pops it. Caller-managed synthetic frames (deferred_pop=FALSE) are untouched by that loop. New `JS_ThrowErrorNoBacktrace` replaces the old `add_backtrace=FALSE` path. tests/oldtests/test_builtin.js gains the upstream-equivalent column-number test cases. tests/_setup.ts gains a sanitizer that strips `:LINE:COL` from `at ...` stack frames in first-base snapshots so engine-line shifts don't churn snapshots. Tests/test262 unchanged in net pass/fail. |
| c361210 | PORT | qjsc: added missing -fno-weakref | Added `{ "weakref", "WeakRef" }` entry to qjsc's feature_list so `-fno-weakref` works. JS_AddIntrinsicWeakRef is already declared in quickjs.h. |
| ecfef71 | PORT | String.prototype.localeCompare is added in JS_AddIntrinsicStringNormalize() as it requires unicode normalization | Moved `localeCompare` table entry out of always-on `js_string_proto_funcs` and into `js_string_proto_normalize` (added by JS_AddIntrinsicStringNormalize). The normalize list is no longer wrapped in `#ifdef CONFIG_ALL_UNICODE`; only the `normalize` entry inside it stays guarded. Both `js_string_localeCompare` definitions (CONFIG_ALL_UNICODE and fallback) kept. Fork callers of JS_AddIntrinsicStringNormalize (qjs runtime, qjsbootstrap, stack-limit-test, quickjs-run, quickjs-context) all unconditionally invoke it, so localeCompare remains available everywhere it was. |
| f5788c7 | SKIP-NA | Define lre_check_timeout in fuzz_regexp | Touches `fuzz/fuzz_regexp.c` only; fork does not vendor `fuzz/`. |
| bff5525 | SKIP-NA | Merge pull request #400 from renatahodovan/fix-regexp | Merge commit for f5788c7 (fuzz/ change). |
| 8bb41b2 | PORT | enabled os.Worker on Windows (bnoordhuis) | Ported the upstream JSWaker abstraction (Windows: manual-reset Event + WaitForMultipleObjects; POSIX: self-pipe) into the fork's split structure. Replaced `JSWorkerMessagePipe`'s `read_fd`/`write_fd` with a `JSWaker waker` and added a `closed` flag (Windows Events have no built-in EOF, so the writer side sets `closed` under the pipe mutex when it wants to signal EOF; the reader checks it after dequeuing). Replaced the fork's `worker_done_read_fd`/`worker_done_write_fd` pair with a refcounted `JSWorkerDoneSignal` (waker + mutex + active_count) — the worker thread decrements active_count under mutex and signals; main reads back active_count under the same mutex. Converted `js_new_message_pipe`, `js_free_message_pipe_local`, `worker_send_error`, `js_worker_set_onmessage` (terminate path), `worker_func` (error pipe close-on-exit), `js_worker_postMessage`, `handle_posted_message`, `handle_posted_error`, and both `js_os_poll` paths (POSIX select and Windows WaitForMultipleObjects). Added a pre-select reaping pass that walks `port_list`/`error_port_list` and unlinks any port whose pipe is `closed && empty queue` — without this, after the parent dispatches the last queued message and drains the waker, a closed pipe would never re-fire and the loop would hang. Tests: added Wine variants in tests/worker-stress, tests/worker-exit, tests/worker-onerror, tests/oldtests/oldtests.test.ts (test_worker.js); added a Wine taskkill test (taskkill /pid by reading a pid file the fixture writes) since SIGKILL kills the wine wrapper not the embedded qjs.exe; added main-exec-with-worker-windows.js fixture using cmd.exe instead of POSIX echo. Test scaffolding: added wineSpawn / setupWineHooks / shouldRunWineTests / shouldRunWasmTests in tests/_utils.ts; converted create-process.test.ts and wasm.test.ts off the test.only-skip-trick to test.runIf so we could drop --allowOnly from package.json. Merged worker-exit2.test.ts contents into worker-exit.test.ts. |
| 8f99de5 | PORT | spec update: ToPropertyKey() is now done after the evaluation of the expression in assignments | Removed eager `OP_to_propkey2`/`OP_to_propkey` emission in `get_lvalue` for `OP_get_array_el`/`OP_get_super_value` in the no-keep branch (the keep-on-stack branch unchanged — that path needs the propkey before the dup2/dup3). Eliminates 7 baseline test262 failures around assignment-target evaluation order. New baseline: 13/40323. |
| 5449fd4 | PORT | more ToPropertyKey ordering changes | (1) JS_GetPropertyValue slow path: throw TypeError for null/undefined this_obj BEFORE ToPropertyKey runs (was: ToPropertyKey ran on prop first, deferring the null/undefined error and clobbering the test262-expected error type). (2) js_parse_object_literal: when name == JS_ATOM_NULL (computed key), emit OP_to_propkey BEFORE js_parse_assign_expr/js_parse_expect — spec requires ToPropertyKey on the key before evaluating the value. Cleared 2 baseline test262 failures (now 11/40323). |
| 1d5e7cf | PORT | fixed destructuring parsing: do it only in assignment expressions | Moved destructuring detection (`{`/`[` followed by `=`) out of `js_parse_postfix_expr`'s `case '{':/case '[':` and into `js_parse_assign_expr2` as a new `else if` branch right before `next:`. Postfix-expr now just delegates to `js_parse_object_literal`/`js_parse_array_literal` unconditionally. Cleared 1 baseline test262 failure (now 10/40323). |
| 83530ac | PORT | fixed destructuring operation order when defining variables - optimized more cases of variable definition in destructuring | New `need_var_reference(s, tok)` helper centralizes the "do we need a runtime variable reference?" check (TOK_VAR + non-strict-or-global, etc.). All four destructuring sites and `js_parse_var` updated to consult it: when a reference is needed they emit OP_scope_get_var + atom + scope_level and goto an `lvalueN:` label that calls get_lvalue with FALSE for keep; when not (let/const), they fast-path with `opcode = OP_scope_get_var; scope = ...; label_lvalue = -1; depth_lvalue = 0;`. The fork's pre-state at one site (the `[` array-destructuring tail) had drifted to omit `enum_depth = 0` and `label_lvalue = -1` — restored to upstream's structure as part of the port. Cleared 1 baseline test262 failure (now 9/40323). |
| 3b04c58 | PORT | fixed 'with' access by adding HasPropery() calls - removed unused 'with_get_ref_undef' opcode | Removed `OP_with_get_ref_undef` from quickjs-opcode.h plus its handlers in JS_CallInternal/resolve_labels/compute_stack_size. Rewrote `OP_get_ref_value` and `OP_put_ref_value` to call `JS_HasProperty` first (matching ObjectEnvironmentRecord GetBindingValue/SetMutableBinding semantics: HasProperty before Get/Set, throw ReferenceError in strict mode if missing). Same HasProperty-then-Get/Set treatment added to `OP_with_get_var`, `OP_with_put_var`, `OP_with_get_ref` cases. Bytecode tests' inline byte snapshots updated by -1 for opcodes past the removed slot (qjsbootstrap-bytecode hardcoded buffer + libbytecode/file-to-bytecode disasm hex). Cleared 5 baseline test262 failures around `with`/proxy-env semantics (now 4/40323). |
| 0c5d59f | PORT | optimized and fixed JS_AtomIsNumericIndex1(): 'NaN' is also a number | Added 4 new atoms (`-0`, `Infinity`, `-Infinity`, `NaN`) and rewrote JS_AtomIsNumericIndex1 as a switch on those atom values + a fast pre-check (first char must be a digit or `-`) before the JS_ToNumber slow path. The pre-existing fork comment about SKIP_WORKER atoms was preserved (just moved past the new atoms). Bytecode-snapshot tests' inline byte arrays + hex disasm dumps shifted by +4 for atom IDs past the insertion point. Cleared 1 baseline test262 failure (now 3/40323). |
| f2b0723 | SKIP-DONE | added 'at' in Array.prototype[Symbol.unscopables] | Fork already has `"at" "\0"` at the top of the unscopables list — pre-ported. |
| 82d86b1 | PORT | removed atom leak introduced in commit 83530ac9 | Three `JS_FreeAtom(s->ctx, var_name)` calls added in `js_parse_destructuring_element` after the `emit_atom(s, var_name)` calls in the new `need_var_reference` paths from 83530ac. emit_atom is consume semantics for the atom in the bytecode stream, but `var_name` is then used again at the `lvalueN:` labels via get_lvalue's out-param — without the free, the atom from `js_parse_destructuring_var` was leaked. |
| b67c416 | PORT | fixed Proxy getOwnPropertyDescriptor with getters and setters | After `js_obj_to_desc` populates `result_desc` from the trap return, normalize the flags before they're used both in the target_desc-comparison branch and in the final `*pdesc` assignment: set `JS_PROP_GETSET` if the desc has accessor flags, `JS_PROP_NORMAL` otherwise; mask down to `JS_PROP_C_W_E | JS_PROP_TMASK`. Without this, code reading `result_desc.flags` couldn't distinguish accessor from data desc by checking `flags & JS_PROP_GETSET`. Test262 baseline unchanged. |
| db3d3f0 | PORT | fixed memory leak in String constructor | When `js_create_from_ctor` returns an exception (e.g. out of memory) inside `js_string_constructor`'s `new String(x)` path, the previously-toString'd `val` was leaked. Free it on the exception path. |
| 000db3a | PORT | the %TypedArray% Intrinsic Object should be a constructor | Switched the `TypedArray` base function from `JS_NewCFunction` to `JS_NewCFunction2(... JS_CFUNC_constructor_or_func, 0)` so it's recognized as a constructor (per spec), even though calling it directly always throws. |
| e7264d6 | PORT | fixed Array.from() and TypedArray.from() | Rewrote both `js_array_from` and `js_typed_array_from`'s iterator branches: replace the old `js_for_of_start`-based stack[2] state with the explicit `JS_GetIterator2` + `JS_GetProperty(JS_ATOM_next)` pair, and add the missing `null`/non-callable @@iterator check (throws TypeError "value is not iterable"). Forward-declares `js_array_from_iterator` so `js_typed_array_from` can call it directly to materialize the iterable into an array. Local `JS_ThrowTypeError` calls adapted to the fork's `(ctx, filename, line, fmt, ...)` signature. |
| e1f6dfe | PORT | fixed checks in Proxy defineProperty | Replaced the fork's looser writable-only check with upstream's two-part validation: (1) reject when target's desc is configurable but caller asked for non-configurable; (2) reject only when the caller explicitly set writable=false but target still reports writable=true (and isn't an accessor). The fork's previous check used a wider mask (`JS_PROP_GETSET | JS_PROP_CONFIGURABLE | JS_PROP_WRITABLE`) and could reject valid configurations. Adapted JS_ThrowTypeError to fork's `(ctx, file, line, fmt, ...)` signature. |
| 37cde16 | PORT | fixed build_arg_list() | Use `js_get_length64` (int64) and check against `JS_MAX_LOCAL_VARS` before truncating to uint32 — previously a length > UINT32_MAX would silently wrap. |
| dbbca3d | PORT | dtoa fix for minus zero | Centralized the `-` sign emission in dtoa: emit it once after the radix-point handling for normal numbers, and gate it on `JS_DTOA_MINUS_ZERO` only in the zero-output path. Previously the sign emission was duplicated across the integer/output paths and incorrectly suppressed `-0` from values that rounded to zero (e.g. `(-1e-10).toFixed(0)` returned `"0"` instead of `"-0"`). Test added in tests/oldtests/test_builtin.js (mirrors upstream's tests/test_builtin.js addition). |
| 334aa18 | PORT | fixed iterator close in Map/Set constructor | Split the existing `fail:` label into `fail_close:` (which calls JS_IteratorClose first) and `fail:` (no close). The two adder-call exception sites now `goto fail_close` so the iterator gets properly closed; everywhere else (where iter wasn't yet opened or already closed) goes straight to `fail`. Removes the `if (JS_IsObject(iter))` guard in the unconditional fail path, which was inadequate. |
| fbf7d8a | PORT | fixed detached TypedArray handling in Atomics operations | RevalidateAtomicAccess: re-check `abuf->detached` after `JS_ToIndex` (which can run user-defined valueOf and detach the buffer mid-operation), throwing TypeError before the index bounds check. |
| e5e7248 | PORT | added staging test262 tests | Added staging test262 exclusions to test262.conf, applied test262.patch updates (atomicsHelper timeout reductions, regExpUtils codePointRange helper, sm/non262.js print() removal), updated test262_errors.txt with the ~108 staging failures (matches upstream's set, fork passes 2 RegExp tests upstream fails). Bundled with this port: removed fork-specific Symbol.typeofValue feature (was causing infinite recursion via typeof reading proxy.get trap in unscopables-proxy.js); fixed pre-existing fork failures (check_function now throws TypeError, "not a function" error message aligned with upstream, JS_ToPrimitiveFree no longer accesses @@toStringTag for the error message). Bytecode atom IDs shifted -1 in test snapshots. |
| c0958ee | SKIP-NA | More CI tragets: Linux 32bit, LTO, Windows and Cosmopolitan | CI/Makefile changes only — fork uses Ninja/meta build (no Makefile), fork-specific .github/workflows/ci_tests.yml, and .gitignore additions are for upstream-root build artifacts that don't apply (fork builds to build/). |
| 99a855f | PORT | future reserved keywords are forbidden in function name and arguments when the function body is in strict mode | Added is_strict_future_keyword() helper; js_parse_function_check_names() now also rejects strict-future-reserved keywords (implements, interface, let, package, private, protected, public, static) as function name or argument name when the function body is strict. Fixes staging/sm/misc/future-reserved-words.js. |
| 5e71d14 | PORT | setters cannot have rest arguments | js_parse_function_decl2: when parsing a setter and encountering TOK_ELLIPSIS (rest parameter), goto fail_accessor (the existing arg-count error path) instead of accepting it as a rest. Fixes staging/sm/object/accessor-arguments-rest.js. |
| 5afd0eb | PORT | fix property ordering in the object returned by RegExp.prototype.exec() | js_regexp_exec: moved the `groups` property definition to after `index` and `input` so the resulting object's enumeration order matches the spec ([0, ..., index, input, groups]). Fixes 3 staging tests (15.2.3.14-01, object/entries, object/values). |
| 1e958ab | PORT | fixed operation order in Object.prototype.propertyIsEnumerable() | js_object_propertyIsEnumerable: convert the property argument to an atom (JS_ValueToAtom) before coercing `this` to an object (JS_ToObject), matching the spec order. Also added an XXX comment in JS_ObjectDefineProperties about its known property-order divergence. Fixes staging/sm/object/propertyIsEnumerable.js. |
| b32cccb | PORT | fixed RegExp.prototype[Symbol.split] | js_regexp_Symbol_split: removed an erroneous JS_ToStringFree() coercion of capture-group sub-strings — they should be propagated as-is (possibly undefined) to match the spec, not coerced to "undefined" strings. Fixes staging/sm/String/{split-01,split-xregexp}. |
| a0a760f | PORT | fixed GeneratorFunction prototype | JS_AddIntrinsicBaseObjects: build the GeneratorFunction constructor with JS_NewCFunction3 passing ctx->function_ctor as the prototype, so its [[Prototype]] is %Function% rather than %Function.prototype% (matches spec — GeneratorFunction's prototype IS Function). Fixes staging/sm/generators/runtime.js. |
| 08a28c0 | PORT | fixed TypedArray.prototype.with with detached ArrayBuffer | js_typed_array_with: throw TypeError up front when the buffer is already detached, and re-check detached after coercing the value (which can run user code that detaches mid-call). The bounds check is consolidated into the post-coercion test so a freshly-detached buffer is reported as the (now-correct) RangeError. Fork-side: kept the fork's JS_ThrowRangeError signature ("<internal>/quickjs.c", __LINE__, ...) on the moved/merged check. Fixes staging/sm/TypedArray/{with,with-detached}. |
| 3bffe67 | PORT | fixed TypedArray.prototype.slice() when the buffers overlap | Added slice_memcpy() helper that uses memcpy for non-overlapping ranges and a byte-by-byte forward copy when source/destination overlap (which the spec mandates for the same-class TypedArray.slice fast path). Replaces the bare memcpy in js_typed_array_slice. Fixes staging/sm/TypedArray/slice-memcpy. |
| 87cf1b0 | PORT | run-test262: added $262.gc() | run-test262.c: added js_gc() (calls JS_RunGC) and exposed it as $262.gc(). Fixes 10 staging tests that explicitly invoke $262.gc() (extensions/recursion, regress-650753, typedarray-set-detach, weakmap, generators/gen-with-call-obj, object/clear-dictionary-accessor-getset, regress/regress-{592556-c35,596103,1507322-deep-weakmap}, statements/for-in-with-gc-and-unvisited-deletion). |
| 8e9e8e8 | PORT | update tests | test262.conf: replaced :line-suffix exclusions with whole-file ones for Set/{is-subset-of,is-superset-of,union}; added more "not supported" / "no f16" / "not standard" exclusions matching upstream (RegExp/{flags,prototype,toString,source}, TypedArray/toString, extensions/{arguments-property-access-in-function,function-caller-skips-eval-frames,function-properties}). Added test262.patch hunk for misc/new-with-non-constructor.js (loosens the error-message text check from " is not a constructor" to "not a constructor" — matches QuickJS's wording). Applied that hunk to the test262 working tree. |
| 3306254 | SKIP-NA | Merge pull request #393 from nickva/more-ci-targets-and-windows-fixes | Merge commit bringing in c0958ee (already SKIP-NA — CI/Makefile only). No additional content. |
| 7645ce5 | PORT | more precise error location reporting | quickjs.c only — added many sf->cur_pc = pc updates in JS_CallInternal opcode handlers (so an exception's reported source position matches the failing opcode, not the prior one), and added emit_source_pos() calls in the parser for unary/postfix/inc/dec/pow and several identifier-lookup paths. Conflict resolved at OP_import (kept fork's JS_DynamicImportAsync) and OP_get_ref_value (kept fork's clean blank line). Bytecode size grew by 4 bytes due to extra OP_line_num emissions; updated 3 inline snapshots accordingly (file-to-bytecode hex view, libbytecode default/source-stripped sizes). |
| 1b13fa6 | PORT | added more C callbacks for exotic objects (#324) | quickjs.h: extended JSClassExoticMethods with get_prototype, set_prototype, is_extensible, prevent_extensions callbacks. quickjs.c: JS_GetPrototype, JS_SetPrototypeInternal, JS_IsExtensible, JS_PreventExtensions, JS_OrdinaryIsInstanceOf now dispatch through these callbacks for any exotic object (not just Proxy); the existing Proxy implementations were renamed to match the callback naming (js_proxy_get_prototype, js_proxy_set_prototype, js_proxy_is_extensible, js_proxy_prevent_extensions) and registered in js_proxy_exotic_methods. js_proxy_set_prototype no longer takes throw_flag — the caller (JS_SetPrototypeInternal) now handles the "proxy: bad prototype" throw. Conflict at the throw site resolved by taking upstream's simpler `if (!res) return FALSE;` and adapting the new throw call in JS_SetPrototypeInternal to fork's 5-arg JS_ThrowTypeError signature. |
| 19abf18 | SKIP-NA | new release | Changelog/Makefile/TODO/VERSION/doc/release.sh — none of these are tracked in the fork (fork uses npm versioning, no Makefile, no upstream release tooling). |
| 894ce9d | SKIP-DONE | fixed js_std_await() so that it behaves the same way as js_std_loop() (#402) | Fork has no js_std_await — only js_eventloop_run (the equivalent of js_std_loop). The bug (poll runs even after JS_ExecutePendingJob returns -1) was specific to js_std_await's per-iteration drain-then-poll shape; fork's js_eventloop_run already uses the drain-fully-then-poll pattern that this commit moves js_std_await toward. |
| 2d4e1cc | PORT | fixed the delete operator with global variables | Added JS_DeleteGlobalVar (per spec 9.1.1.4.7 DeleteBinding): if the global lexical declarations table has the binding, return FALSE (lexical bindings can't be deleted); otherwise delete from global_obj if present, else return TRUE. OP_delete_var now calls JS_DeleteGlobalVar instead of bare JS_DeleteProperty. Fixes staging/sm/lexical-environment/unscopables-proto.js. |
| 9bb1d72 | PORT | fixed operation order in js_obj_to_desc() | Reordered the property accesses in js_obj_to_desc to match the spec: enumerable, configurable, value, writable (was: configurable, writable, enumerable, value). The relative order matters because each access can run user code on a Proxy, and the spec mandates the visible call order. Fixes staging/sm/object/defineProperty-proxy.js. |
| 3fbea36 | PORT | update tests | test262.conf: added 2 more staging exclusions (RegExp/escape and syntax-parsed-arrow-then-directive — both for not-yet-standard features upstream skips). |
| 23e2dc9 | PORT | handle strings as module import and export names | Parser: js_parse_export and js_parse_import now accept TOK_STRING for the `as` name and the imported binding name (rejecting strings with unpaired surrogates). add_import gained an explicit is_star parameter so the namespace-import flag is no longer derived from import_name == "*", letting "*" be a legitimate user-chosen name. JSImportEntry gained an is_star flag. BC_BASE_VERSION bumped 4→5 (fork keeps its endianness-flag scheme). Read/write of import entries serializes the new flag. Conflict at the version macro resolved by bumping fork's BC_BASE_VERSION. Bytecode atom-shift: regenerated qjsbootstrap-bytecode.test.ts hardcoded buffer (only first byte changed, 4→5) and re-snapshotted file-to-bytecode/libbytecode tests. Fixes staging/sm/module/module-export-name-star.js. |
| 53327c2 | PORT | simplified uncatchable exception handling | Replaced per-Error-object is_uncatchable_error flag with a single per-runtime current_exception_is_uncatchable bit (so the flag now applies to whatever exception is currently in flight, not to a specific Error object). Public API: removed JS_IsUncatchableError, JS_SetUncatchableError, JS_ResetUncatchableError; added JS_SetUncatchableException(ctx, flag). Updated fork's quickjs-eventloop.c interrupt-handler to use the new API. JS_Throw resets the flag to FALSE on each new throw. JS_ThrowInterrupted now sets it. Conflict at JS_ThrowInterrupted resolved by keeping fork's 5-arg JS_ThrowInternalError signature alongside the new SetUncatchableException call; conflict at the JS_CallInternal exception-unwind resolved by keeping fork's deferred-pop synthetic-frame logic and only swapping the catchability check. |
| 30fe3de | PORT | Object.prototype has an immutable prototype | Added has_immutable_prototype bit on JSObject and JS_SetImmutablePrototype helper. JS_SetPrototypeInternal rejects modification with TypeError "prototype is immutable" (or returns FALSE if !throw_flag). JS_AddIntrinsicBasicObjects marks Object.prototype as immutable. New throw site uses fork's 5-arg JS_ThrowTypeError signature. |
| be06b3e | PORT | added JS_PrintValue() and use it in console.log(), print() and the REPL (#256) | C API ported verbatim with upstream signatures (`JS_PrintValue`, `JS_PrintValueRT`, `JS_PrintValueSetDefaultOptions`, `JSPrintValueOptions`). Printer's output sink refactored to dual `FILE *` / `DynBuf *` via fork-internal `JS_PrintValueToSink[RT]` helpers (fork addition) — the upstream-signature wrappers stay trivial so future upstream ports apply cleanly. Applied the print rewires for fork's `js_print` (in src/globals/quickjs-print/quickjs-print.c) and run-test262's `js_print`: non-string args now format via `JS_PrintValue` rather than producing `[object Object]`. Skipped upstream's REPL rewire — fork's REPL keeps using `inspect()` which is more capable for that purpose. Relocated upstream's new `__printObject` helper from `std` to `quickjs:engine` (fork has been moving std helpers to engine — e.g. `gc()`); kept the `__` prefix as a marker that it mirrors the upstream API. Added new fork-side JS export `engine.formatValue(value, options?)` returning a string, exposing the C-driven formatter to JS callers (showClosure, rawDump, etc.). Adapted fork's `QJU_PrintError` non-Error non-string fallback to use `JS_PrintValue` (parallel improvement). Drive-by: fixed REPL `std.exit(0)` → `cmdline.exit(0)` (fork moved exit out of std long ago; `\q` directive was throwing). Re-snapshotted 8 tests with legitimate drift from the new `console.log`/`print` behavior. Added 16 new format-value tests + 9 new print/console.log tests. Tests: 482 passing (was 456). Test262: 63 errors (no change from baseline). |
| 703de06 | SKIP-DONE | fixed use of JS_DumpValue() | Upstream fix-up to `be06b3e` for two debug call sites that still used the old 2-arg JS_DumpValue form. Already addressed in fork's port of be06b3e (commit fa2051b): both sites converted to JS_PrintValue(ctx, stdout, val, NULL) during conflict resolution. Functionally equivalent to upstream's fix. |
| 11d076f | PORT | added get_array_el3 opcode - removed to_propkey2 opcode | New `OP_get_array_el3` (1/2/3) replaces the `to_propkey2`+`dup2`+`get_array_el` sequence emitted by `get_lvalue` for `OP_get_array_el` lvalues. `OP_to_propkey2` and its CASE block removed; the destructuring-element path that used it now uses plain `OP_to_propkey` (the null/undefined check is folded into the new opcode's body). Manual conflict resolution: kept fork's 5-arg `JS_ThrowTypeError(ctx, "<internal>/quickjs.c", __LINE__, ...)` signature in the new opcode body. resolve_labels' optimization comment updated to drop the `to_propkey2` reference. Snapshot updated for tests/file-to-bytecode.test.ts (opcode renumbering only). |
| 0a6160d | PORT | avoid relying on 'FILE *' in JS_PrintValue() API | Adopted upstream's new write-callback API: `JS_PrintValue` / `JS_PrintValueRT` now take `(JSPrintValueWrite *write_func, void *write_opaque)` instead of `FILE *`. Added `JSPrintValueWrite` typedef in `quickjs.h`. The fork's prior dual-sink shim (`fo`/`dbuf` fields on `JSPrintValueState`, plus `JS_PrintValueToSink[RT]` and the `js_print_sink_*` dispatch helpers) collapses cleanly: kept the helper names but rewrote bodies to forward to the write callback. Removed `JS_PrintValueToSink[RT]` from the public C API (callers now compose a small DynBuf-writing callback instead). **Preserved `show_closure`** as a fork-added option on `JSPrintValueOptions` (upstream removed it and merged its functionality into `raw_dump`); the `js_print_object` check still uses `s->options.show_closure` so the JS-side `engine.formatValue({ showClosure: true })` keeps working unchanged. Added the `js_dump_value_write` FILE-writing callback adapter used by all internal stdout/stderr dump paths (`JS_DumpValue`/`JS_DumpValueRT`/`JS_DumpObject`/`dump_byte_code`/`print_atom`/the GC free trace at line 5829/the DUMP_PROMISE trace at line 50219). Updated 4 fork-side callers (each gets its own small `*_print_value_write` callback that wraps `fwrite` to a FILE *): `quickjs-print` (`js_print` in `src/globals/quickjs-print/quickjs-print.c`), `run-test262` (`js_print` in `src/run-test262/run-test262.c`), `quickjs-engine` (`formatValue` now uses a DynBuf-writing callback `js_engine_format_value_dbuf_write`; `__printObject` uses `js_engine_print_value_write`), `quickjs-utils` (`QJU_PrintError`'s non-Error non-string fallback uses `qju_print_value_write`). `engine.formatValue`'s `.d.ts` unchanged — `showClosure` JS option still works. Tests: 482 passing. Test262: 64 errors (no change). |
| c95b024 | PORT | added RegExp.escape (bnoordhuis) | Auto-applied cleanly to `src/quickjs/quickjs.c` and `src/run-test262/test262.conf`. Adapted upstream's 2-arg `JS_ThrowTypeError(ctx, "not a string")` to fork's 5-arg signature. test262.conf flag flipped from `RegExp.escape=skip` to enabled — 20 previously-skipped tests now run and all pass (no diff in test262_errors.txt). |
| a8b2d7c | PORT | added Float16Array (bnoordhuis) - optimized float16 conversion functions | Auto-applied with one resolved conflict in `quickjs.c`: the fork has moved the `JS_CLASS_*` enum from `quickjs.c` to `quickjs.h`, so the patch's enum hunk landed as a duplicate inside `quickjs.c`. Removed the duplicate enum block; manually added `JS_CLASS_FLOAT16_ARRAY` in the right place in the fork's `src/quickjs/quickjs.h` enum (between BIG_UINT64_ARRAY and FLOAT32_ARRAY). Other parts applied cleanly: cutils.h gains `fromfp16`/`tofp16`/`isfp16nan`/`isfp16zero` helpers; quickjs-atom.h gets the `Float16Array` atom; quickjs.h gets `JS_TYPED_ARRAY_FLOAT16` enum entry; quickjs.c gets the `js_math_f16round`, `js_TA_cmp_float16`, `js_TA_get_float16` helpers and Float16Array handling in JS_GetPropertyValue / JS_SetPropertyValue / js_print_object / js_typed_array_fill / js_typed_array_indexOf / js_typed_array_sort / js_dataview_getValue / js_dataview_setValue plus `getFloat16`/`setFloat16` DataView methods; test262.conf flips `Float16Array=skip` to enabled and removes 4 SM staging test exclusions; test262_errors.txt drops `sort-negative-nan.js`. **Skipped:** TODO (not vendored), tests/test_bjson.js (not vendored — fork's `tests/` is vitest, not engine tests). **Manually applied** the test_builtin.js Float16Array test addition to `tests/oldtests/test_builtin.js` (fork's relocated upstream-style test). Updated 6 vitest snapshots (5 bytecode/atom-id snapshots shifted because `JS_ATOM_Float16Array` insertion renumbered atoms; globals.test.ts adds the new global). Tests: 482 passing. Test262: 63 errors (was 64; `sort-negative-nan.js` now passes), 41605 tests run (was 41554). |
| d7cdfdc | PORT | regexp: added v flag support - fixed corner cases of case insensitive matching | Large port (~2000 lines). libregexp.c: gains v-flag (`unicode_sets`) parsing + class-set operations + `\q{...}` string disjunctions; fixed corner cases in case-insensitive matching. libunicode.h/c: `cr_union1` → `cr_op1(..., op)`, added `CR_OP_SUB` and `unicode_sequence_prop` for parsing emoji sequences. unicode_gen.c: parses new emoji-sequences.txt and emoji-zwj-sequences.txt to emit RGI emoji property tables. Manually resolved 5 conflicts: (1) **libregexp.c**: dropped fork's special-case escape fallback `if (ret == -2 && strchr("^$\\.*+?()[]{}|/-", *p))` — upstream's new code adds explicit per-character cases (including `-`), making the fallback redundant. (2) **quickjs-atom.h**: added 6 new regexp flag atoms (`hasIndices`, `ignoreCase`, `multiline`, `dotAll`, `sticky`, `unicodeSets`) above the worker/atomics atoms — kept fork's `SKIP_WORKER` comment, dropped upstream's `CONFIG_ATOMICS` comment that the fork doesn't use. (3) **quickjs.c bad_flags**: switched to `goto bad_flags1` (so the new u/v incompatibility check below can reuse the label) — kept fork's 5-arg `JS_ThrowSyntaxError` signature at the label. (4-5) **quickjs.c Symbol.match / Symbol.replace**: kept fork's upfront `flags` string scan (vs upstream's deferred scan inside the `is_global` branch) but extended `fullUnicode` to also test for `'v'`. **test262**: removed 12 fork-specific `regexp-v-flag is missing in the tests` exclusions (now supported); test262_errors.txt drops 2 entries (`unicode-ignoreCase-escape.js`, `unicode-ignoreCase-word-boundary.js`). **Manual:** applied test_builtin.js change to fork's `tests/oldtests/test_builtin.js`. Wired the 2 new emoji-sequence files into `libunicode.ninja.js` implicit inputs after running `unicode_download.sh` to fetch them. **Skipped:** libunicode-table.h (fork generates it at build via `unicode_gen.c`). 5 vitest snapshots updated for atom-id renumbering. Tests: 482 passing. Test262: 61 errors (was 63), 41805 tests run (was 41605) — net 2 fixes plus 200 more tests now run thanks to v-flag support. |
| 3c39307 | PORT | better promise rejection tracker heuristics (#112) | Manual port (upstream's quickjs-libc.c → fork's split: tracker logic lives in `quickjs-eventloop`). Defers rejection reporting until the next event loop sleep point — rejections are recorded on `JSThreadState.rejected_promise_list` when fired with is_handled=FALSE and removed when fired with is_handled=TRUE, so a rejection caught synchronously before the loop yields never produces output. **eventloop**: added `JSRejectedPromiseEntry` struct in `quickjs-eventloop.h`, new `rejected_promise_list` field on `JSThreadState`, init in `js_eventloop_init`, free remaining entries in `js_eventloop_free`. New `qju_eventloop_promise_rejection_tracker` (host tracker callback) plus internal `find_rejected_promise` and `js_eventloop_promise_rejection_check` helpers — the check fires once per loop iteration before `js_poll_func`, prints all leftover entries via `QJU_PrintError` and exits(1). **quickjs-utils**: deleted the now-unused `QJU_PrintPromiseRejection` (the synchronous "print and exit on first" tracker) — it can't access JSThreadState across the dep boundary, so the new deferred logic naturally moves to eventloop. **Programs**: switched 4 programs from `QJU_PrintPromiseRejection` to `qju_eventloop_promise_rejection_tracker` (qjs.c, quickjs-run.c, qjsbootstrap.c, stack-limit-test/main.c); added missing `#include "quickjs-eventloop.h"` to qjs.c, quickjs-run.c, qjsbootstrap.c (stack-limit-test already had it). Worker tracker (`worker_promise_rejection_tracker` in quickjs-os.c) **not changed** — it routes via QJU_ReportException to the parent error pipe, a different policy from the main thread's print/exit. **Tests**: applied the upstream test addition to fork's `tests/oldtests/test_std.js` (substituted `setTimeout` for `os.setTimeout` since the fork's `os` module no longer exposes `setTimeout` — moved to `quickjs:timers` and exposed as a global). Updated one inline snapshot for the C26 test in tests/unhandled-rejection.test.ts: previous behavior reported only the first rejection; new behavior reports both before exiting (intentional improvement from the deferred design). Tests: 482 passing. Test262: 61 errors (no change). |
| 9c973a8 | PORT | added Promise.try (saghul) | Auto-applied cleanly to `src/quickjs/quickjs.c` (new `js_promise_try` C function + `try` entry in `js_promise_funcs[]`) and `src/run-test262/test262.conf` (flipped `promise-try=skip` to enabled). 12 previously-skipped test262 tests now run and pass (no diff in test262_errors.txt). |
| f95b8ba | PORT | added regexp modifiers | Auto-applied cleanly to `src/lib/libregexp/libregexp-opcode.h`, `src/lib/libregexp/libregexp.c`, and `src/run-test262/test262.conf` (`regexp-modifiers=skip` flipped to enabled). 230 previously-skipped test262 tests now run with no new failures. |
| a33610d | SKIP-NA | update test results | TODO file only; not vendored (fork uses todo.txt). |
| 1021e3c | PORT | compilation fix | Auto-applied cleanly to `src/quickjs/quickjs.c`. Tightens `js_promise_try`'s param types to `JSValueConst` and adds a cast at a JS_Call site (follow-up to 9c973a8). |
| aaa9cea | PORT | Proxy: fixed prototype comparison in setPrototypeOf() and getPrototypeOf() (#410) | Auto-applied cleanly to `src/quickjs/quickjs.c`. Replaces `JS_VALUE_GET_OBJ(a) != JS_VALUE_GET_OBJ(b)` with `!js_same_value(ctx, a, b)` in two proxy prototype-comparison sites (handles non-object proto values like null correctly). Test262 drops 1 error: `staging/sm/Proxy/getPrototypeOf.js` now passes (60 errors, was 61). |
| 9bce51e | PORT | improved JSON parser conformity (chqrlie) (#250) | Auto-applied to `src/quickjs/quickjs.c` cleanly. Resolved one conflict in `src/run-test262/test262_errors.txt` (fork has 2 JSON-parse-syntax errors that upstream removed; fork doesn't have `Math/cbrt-approx.js`). Skipped applying the upstream `tests/test_builtin.js` change wholesale — fork's `assert_json_error` has a different signature (line/col args), so the upstream string substitution doesn't transfer; instead bumped the existing fork test's column expectation from 4 to 5 to match the new parser's reported position. **Skipped:** TODO update (not vendored). Test262: 58 errors (was 60); both `JSON/parse-number-syntax.js` and `JSON/parse-syntax-errors-02.js` now pass. |
| 2f167bb | PORT | export JS_FreePropertyEnum() | Auto-applied to `src/quickjs/quickjs.c` and `src/quickjs/quickjs.h` cleanly (renames `js_free_prop_enum` → `JS_FreePropertyEnum` and exports it). Manually applied the upstream `quickjs-libc.c` `build_envp` change to fork's split location: `src/builtin-modules/quickjs-os/quickjs-os.c` build_envp `done:` block now calls `JS_FreePropertyEnum(ctx, tab, len)` instead of the open-coded loop+free. |
| 8381245 | PORT | added JS_AtomToCStringLen() | Auto-applied cleanly. New `JS_AtomToCStringLen(ctx, plen, atom)` exposed in quickjs.h; existing `JS_AtomToCString` is now an inline forwarder. Required a clean rebuild (incremental build couldn't link — `JS_AtomToCString` switched from extern to inline so dependent .o files needed recompilation). One bytecode-trace snapshot shifted (line 7331 → 7334 due to 3 new lines in the function). |
| f10ef29 | PORT | added JSON modules and import attributes | Full integration into the fork's modulesys (option B, not just C-engine hooks). Four atomic commits A–D: (A) C-engine ABI: `JSModuleLoaderFunc2`, `JSModuleNormalizeFunc2` (fork divergence — `JS_SetModuleLoaderFunc2` takes v2 normalize+loader, unlike upstream which keeps normalize as v1), `JSModuleCheckSupportedImportAttributes`, getters; `JSReqModuleEntry.attributes`, `JSModuleDef.private_value` and `JSModuleDef.attributes`; `add_req_module_entry` takes attributes (no dedup-by-name); parser support for `with { ... }` clause and `import(specifier, options)`; `OP_import` arity 1→2; `JS_DynamicImport*` widened. (B) Modulesys: permissive `qjms_module_check_attributes` (any keys accepted; spec interpretation is delegated to `ModuleDelegate.compilers`); `QJMS_ModuleLoader` and `QJMS_NormalizeModuleName` v2 signatures; `QJMS_SetModuleImportMeta` populates `import.meta.attributes` as a defensive copy with non-writable+non-configurable own properties on a non-extensible object (descriptor itself non-writable+non-configurable; `undefined` when no `with` clause); `QJMS_InitState` switches to `JS_SetModuleLoaderFunc2`; `QJMS_Require/Require2/RequireResolve/RequireResolve2` widened in-place to take attributes (fork API breaking change); `js_require`/`js_require_resolve` accept `(spec, options)`; `module-impl.js` ModuleDelegate restructured — `compilers["json"]` pre-registered (no leading dot — match by import-attributes type), `read(name, attributes)` looks up by attribute type first then file extension, `resolve` accepts attributes; `engine.importModule`/`engine.resolveModule` accept `options` 3rd arg. Attribute-aware caching: `js_find_loaded_module` compares (name, attributes) via `attributes_equal`, so same-specifier-different-attributes imports are distinct module instances per spec; `js_find_loaded_module_by_name` retained for `import.meta` lookup which lacks attribute context. Bytecode format bumped to v6 to serialize/deserialize `JSReqModuleEntry.attributes`. New `tests/import-attributes.test.ts` (18 cases A–L) covering attribute-driven dispatch, extension-only opt-in, `import.meta.attributes` locked-down semantics, multi-key non-`type` attrs, caching with same and different attributes, `require`/`import.meta.require`/dynamic `import()`/`engine.importModule` options, and `read`/`resolve`/compiler error propagation. Bumped 4 bytecode-hexdump snapshots for BC v5→v6 drift. (C) `qjsc.c` and `run-test262.c` switched to v2 install with permissive checker; both perform a JSON-source rewrite (`export default JSON.parse(<JSON.stringify(content)>);`) when `attributes.type === "json"`, mirroring the runtime modulesys behavior. (D) `import-assertions`, `import-attributes`, `json-modules` test262 features enabled. 89 previously-skipped tests now run; 79 pass and 10 fail — the new failures are spec edge cases (strict whitelist on non-`type` keys, abrupt-completion semantics during options enumeration, parser-level dup-key check) and are deliberate fork divergences arising from the permissive checker. Net: +10 errors (58 → 68) but +79 newly-passing tests. Upstream chose name-only caching; the fork chose attribute-aware caching for spec-compliance. |
| 071a4cf | PORT | use Object.is() in tests | Applied `Object.is(actual, expected)` to the inlined `assert` helpers in `tests/oldtests/test_language.js` and `tests/oldtests/test_std.js`. Upstream's shared `tests/assert.js` has no fork equivalent (fork inlines the helper in each test file). |
| 8b2a124 | PORT | fixed Regexp.prototype[Symbol.match] | In `js_regexp_Symbol_match` (`src/quickjs/quickjs.c`), switched the match-result append from `JS_SetPropertyInt64` to `JS_DefinePropertyValueInt64(..., JS_PROP_C_W_E \| JS_PROP_THROW)` so the element is created with full configurable/writable/enumerable attributes (spec-correct DefinePropertyOrThrow semantics). Test262 errors went 68 → 67: `staging/sm/String/match-defines-match-elements.js` now passes. |
| 1dfaa61 | PORT | improved compatibility of std.parseExtJSON() with JSON5 | Added JSON5 features to ext_json mode in `src/quickjs/quickjs.c`: NaN/Infinity number literals, leading-dot decimals, multi-line strings (escaped newline), and `\v` escape; updated `tests/oldtests/test_std.js` to cover the new cases. |
| 7c487f1 | PORT | support JSON modules in qjsc - added support of JSON5 modules (using type = "json5") | Adapted to the fork's JS-source-rewrite JSON-module approach (distinct from upstream's `JS_ParseJSON2`-into-dummy-CModule design): registered `compilers["json5"]` in `src/quickjs-modulesys/module-impl.js` (emits `import * as std from "quickjs:std"; export default std.parseExtJSON(...)`), and extended the matching JSON-source-rewrite paths in `src/programs/qjsc/qjsc.c` and `src/run-test262/run-test262.c` to recognize `type: "json5"`. Updated `quickjs-modulesys.d.ts` and added `tests/fixtures/import-attributes/data.json5` + a new vitest case. Bytecode-attribute-serialization hunk in `quickjs.c` is SKIP-DONE — the fork already serializes import attributes through an atom-pair scheme in `JS_WriteModule`/`JS_ReadModule`. Examples/* (`hello_module.js`, `message.json`) not vendored. |
| 1572aa8 | PORT | avoid win32 crash in getTimezoneOffset() if time is < 1970 (#238) | Added NULL checks for gmtime/localtime return values in `getTimezoneOffset` in `src/quickjs/quickjs.c` — on win32 these return NULL for time_t values before 1970 and dereferencing would crash. |
| 3dc7ef1 | PORT | more robust out of memory handling (#406) | Applied upstream's OOM-resilience checks across `src/quickjs/quickjs.c` and `src/quickjs/quickjs.h`: added JS_ATOM_NULL handling on `JS_NewAtom` in `JS_NewCFunction3`/`JS_GetPropertyStr`/`JS_SetPropertyStr`/`JS_DefinePropertyValueStr`; checked `string_buffer_end` exceptions in `js_parse_template_part`/`js_parse_string`/`js_parse_regexp`; restructured `new_label_fd`/`new_label`/`emit_atom` to surface OOM via `dbuf_set_error`; added bounds check in `free_bytecode_atoms`; changed `JS_SetPropertyFunctionList` to return `int`. Three conflicts in `quickjs.c` resolved by preserving the fork's `build_backtrace` / `JS_ThrowError2` restructure (deferred-backtrace + first-frame-info capture); transcribed the spirit of upstream's OOM-bailout into the fork's tail-side `JS_DefinePropertyValue` calls via a `skip_first_frame_info:` goto. Required a clean rebuild (incremental build hit the `JS_SetPropertyFunctionList` ABI change in `quickjs.h` and silently linked stale `.o` files, producing a wasm `signature_mismatch` at runtime — protocol's documented gotcha). |
| 02a2643 | PORT | fixed parsing of function definition | One-line fix in `js_parse_function_decl2` in `src/quickjs/quickjs.c` — only treat `TOK_ARROW` as the start of an arrow body when `func_type == JS_PARSE_FUNC_ARROW`, so e.g. `{a() => 0}` is correctly rejected as a syntax error. Fixes `staging/sm/class/methDefn.js` (test262 errors 67 → 66). |
| 3d92a9d | PORT | new keyword cannot be used with an optional chain | Added check in `js_parse_postfix_expr` in `src/quickjs/quickjs.c`: throw "new keyword cannot be used with an optional chain" SyntaxError when `TOK_QUESTION_MARK_DOT` is encountered without `PF_POSTFIX_CALL`. Fixes `staging/sm/expressions/optional-chain.js` (test262 errors 66 → 65). |
| dfc254a | PORT | update tests | Added 8 `staging/sm/regress/regress-*.js` excludes to `src/run-test262/test262.conf` and a `regress-469625-01.js` test patch to `src/run-test262/test262.patch` (path remapped from upstream's `tests/test262.patch`). One conflict in test262.patch — stray merge markers around an unrelated hunk insertion — resolved by removing the markers. `TODO` is not vendored (fork uses `todo.txt`). test262 errors 65 → 57. |
| fc524f7 | PORT | added missing 'Unknown' unicode Script | Patched `src/lib/encoding/libunicode/unicode_gen.c` (drop the `i = 1` offset that was skipping the leading `Unknown` script in the generated name table) and `src/lib/encoding/libunicode/libunicode.c`. The upstream `libunicode-table.h` hunk is SKIP-DONE — the fork generates that file at build time from `unicode_gen.c`, so the regenerated output picks up the change automatically. Verified `Unknown,Zzzz` is now the first entry in `build/intermediate/libunicode-table.h`. |
| bb986e5 | PORT | update tests | Added `upsert=skip` to `src/run-test262/test262.conf` (forward-compat for a future test262 upgrade — no upsert-tagged tests exist at the fork's current test262 commit, so the skip is currently a no-op). `TODO` is not vendored. The upstream `test262_errors.txt` diff is also not applicable — upstream's recorded errors reflect their test262 advance to commit 4b5d36ab, but the fork is at 56e77d63; errors stay at 57. |
| 638ec8c | PORT | fixed js_bigint_to_string1() (#412) | Fixed BigInt-to-string conversion in `js_bigint_to_string1` in `src/quickjs/quickjs.c`. |
| 00b1d8d | PORT | Read byteOffset for detached buffers | In `js_typed_array_subarray` in `src/quickjs/quickjs.c`, read the typed array's stored offset directly (`ta->offset`) instead of going through `js_typed_array_get_byteOffset`, which throws on a detached buffer. Per spec, byteOffset is still readable from a detached buffer. Fixes `staging/sm/TypedArray/subarray.js` (test262 errors 57 → 56). |
| 0f7eadf | SKIP-NA | Fix Windows MinGW CI Build | Edits `.github/workflows/ci.yml`. The fork has its own CI in `.github/workflows/ci_tests.yml`, structured differently. |
| 9b935db | SKIP-DONE | Merge pull request #418 from nickva/fix-byteoffset-for-detached-array-buffers | Merge commit pulling 00b1d8d and 0f7eadf together; both already processed. |
| 098f221 | PORT | added Error.isError() (bnoordhuis) | Added `js_error_isError` (wrapping `JS_IsError`) and the `Error.isError` static method registration in `src/quickjs/quickjs.c`. Removed `Error.isError=skip` from `src/run-test262/test262.conf` so the feature group runs. `TODO` is not vendored. No fork `.d.ts` change needed - the fork's `quickjs.d.ts` does not declare `Error` (TS lib provides it). |
| 4d9a27c | SKIP-NA | update Changelog | Changelog is not vendored in the fork. |
| f1b1c00 | PORT | update test262 | Applied the `test262.conf` exclude for `staging/sm/Function/function-toString-builtin.js` (the "bound fn" toString form is permitted by spec). The `tests/test262.patch` hunk is SKIP-NA — upstream is re-anchoring its patch's line-number contexts against a newer test262 commit, but the fork is at a different test262 commit (56e77d63) and its `src/run-test262/test262.patch` is already valid against that. `TODO` not vendored. test262 errors 56 → 55. |
| 2fd48bf | PORT | fixed module async evaluation logic - added DUMP_MODULE_EXEC | Fixed module async evaluation logic in `src/quickjs/quickjs.c` (renamed/clarified async_evaluation tracking and added DUMP_MODULE_EXEC debug flag). `TODO` not vendored. Fixes `module-graphs-does-not-hang.js`; test262 errors 55 → 54. |
| 458c34d | PORT | fixed GC logic so that a module can live after a JSContext is destroyed (#280) - update the reference count for the realm in jobs and FinalizationRegistry | Big patch across `src/quickjs/quickjs.c`, `src/run-test262/run-test262.c`, and `src/quickjs-eventloop/quickjs-eventloop.c`. Two `quickjs.c` conflicts: the fork's extra `m->attributes` and `m->user_data` comment in `js_free_module_def` were merged into upstream's restructured RT-form (using `JS_FreeValueRT`); the `frd->ctx` → `frd->realm` rename followed upstream. The fork's `quickjs-libc.c` equivalents (`js_std_loop`, `js_std_await`) don't exist; the equivalent eventloop path is in `src/quickjs-eventloop/quickjs-eventloop.c` (uses `QJU_ReportException` instead of `js_std_dump_error`), updated analogously to pass NULL to `JS_ExecutePendingJob` and use `ctx` for exception reporting. One conflict in `run-test262.c` (`QJU_PrintException(ctx1, ...)` → `QJU_PrintException(ctx, ...)`). test262 errors stay at 54. |
| 1fdc768 | SKIP-NA | removed module leak in js_std_eval_binary() (#425) | `js_std_eval_binary` doesn't exist in the fork; the bytecode-loading API is `quickjs:bytecode.evalBytecode` (in `src/builtin-modules/quickjs-bytecode/quickjs-bytecode.c`) which returns a bound-function wrapper for the JS caller to invoke and frees `obj` cleanly. No analogous leak path. |
| bb34e27 | SKIP-NA | test262 update | Updates `TODO` (not vendored) and records new test262 errors against an advanced upstream test262 commit (e7e13675). Fork is on a different test262 commit; its `test262_errors.txt` is regenerated by `-u` after each port. No fork-side change needed. |
| c942978 | PORT | TypedArray.prototype.subarray: fixed the step at which '[[ByteOffset]]' is read | Reordered the read of `ta->offset` to happen before the second coercion in `js_typed_array_subarray` in `src/quickjs/quickjs.c`. |
| e1c18be | PORT | fixed buffer overflow in js_bigint_from_string() | Added length sanity-check before allocating in `js_bigint_from_string` in `src/quickjs/quickjs.c`, and the matching `js_atof` exception-handling cleanup. Adapted upstream's `JS_ThrowRangeError(ctx, fmt)` call to the fork's `JS_ThrowRangeError(ctx, filename, line, fmt)` signature. |
| 1168c21 | PORT | fixed crash in OP_add_loc if the variable is modified in JS_ToPrimitiveFree() | Small fix in the `OP_add_loc` handler in `src/quickjs/quickjs.c` to guard against the local being modified during `JS_ToPrimitiveFree`. |
| 9ce5442 | PORT | fixed buffer overflow in js_bigint_to_string1() | Buffer-overflow guard in `js_bigint_to_string1` in `src/quickjs/quickjs.c`. |
| c927eca | PORT | fixed buffer overflow in TypedArray.prototype.lastIndexOf() | Overflow guard in `TypedArray.prototype.lastIndexOf` in `src/quickjs/quickjs.c`. |
| 4e0d0b7 | PORT | avoid side effects in JS_PrintValue() which may lead to crashes in print() and js_std_promise_rejection_check() | Substantial refactor in `src/quickjs/quickjs.c`: renamed `get_func_name` → `get_prop_string` (now takes an atom arg, and looks up one level of prototype); added `js_print_regexp` and `js_print_error` helpers that avoid invoking arbitrary JS during printing; replaced the `JS_CLASS_REGEXP` / `JS_CLASS_ERROR` print branches that previously called `js_regexp_toString` / `js_error_toString` (both side-effecting). Four conflicts: (1) `build_backtrace` synthetic-frame handling kept atop the upstream rename of `get_func_name` → `get_prop_string`; (2) `js_print_raw_string` body uses fork's `js_print_sink_write` instead of `s->write_func`, dropped the `remove_last_lf` param upstream removed; (3) the `[Function ...]` branch follows the rename; (4) the Error-printing branch fully replaced by `js_print_error`. The two new upstream helper functions (`js_print_regexp`, `js_print_error`) were translated from upstream's `js_putc`/`js_puts`/`s->write_func` direct calls to the fork's `js_print_sink_putc`/`js_print_sink_puts`/`js_print_sink_write` indirection. |
| d9ec8f1 | PORT | limit function and regexp bytecode to 1G to avoid buffer overflows (the bytecode generators assume that bytecode offsets can fit a 32 bit signed integer | Added 1G bytecode-size caps in `src/quickjs/quickjs.c` and `src/lib/libregexp/libregexp.c`. |
| a4ac84d | PORT | Adjust lastIndex to leading surrogate when inside a surrogate pair in unicode RegExp (initial patch by auvred) | In `lre_exec` in `src/lib/libregexp/libregexp.c`, snap `cptr` back to the leading surrogate when `lastIndex` points inside a surrogate pair under a unicode RegExp. Added the new test_regexp cases to `tests/oldtests/test_builtin.js`. |
| 5689f30 | PORT | fixed handling of 8 bit unicode strings in RegExp (regression introduced by commit a4ac84d) | One-line follow-up to a4ac84d in `src/lib/libregexp/libregexp.c`. |
| 0b3c73e | PORT | removed function cast warnings (initial patch by saghul) | Cast cleanups in `src/quickjs/quickjs.c` and `src/quickjs/quickjs.h`. |
| a4e4b43 | PORT | run-test262: added --count_skipped_features option | Added the option in `src/run-test262/run-test262.c`. |
| 20d2b40 | PORT | qjsc: handle C name conflicts between scripts and modules (#432) | In `src/programs/qjsc/qjsc.c`, call `find_unique_cname` on the script-input branch too so a script-derived cname won't collide with a previously-registered module-derived one. |
| 8a0a6e9 | PORT | better pretty printing of strings - removed String.prototype.__quote() | Replaced upstream's old `js_dump_char` per-char approach with the new `js_print_string1` (whole-string, surrogate-pair-aware, more escape rules) in `src/quickjs/quickjs.c`. Three conflicts: the fork's sink-aware versions (`JS_DumpCharSink`, `js_print_sink_putc`) were updated to use `js_print_string1` with `js_print_sink_*` API; `JS_DumpCharSink` is now unused and removed. Removed `String.prototype.__quote` (unused in fork). `tests/test_bjson.js` not vendored. |
| 9f6c190 | PORT | more efficient handling of strings in JSON.stringify() | JSON-stringify string-handling efficiency refactor in `src/quickjs/quickjs.c`. |
| 391cd3f | PORT | Fix crash on failure to read bytecode (penneryu) | One-line crash fix in bytecode reader in `src/quickjs/quickjs.c`. |
| fa628f8 | SKIP-NA | new release | Touches `Changelog` and `VERSION`, neither vendored by fork. |
| dc7af0a | SKIP-NA | updated release.sh | `release.sh` not vendored. |
| de4d392 | PORT | removed memory leak (#441) | Memory leak fix in `src/quickjs/quickjs.c`. |
| a1e073e | PORT | added set methods (bnoordhuis) | Added 7 new Set.prototype methods (intersection, union, difference, symmetricDifference, isDisjointFrom, isSubsetOf, isSupersetOf) plus js_setlike_get_{size,has,keys} helpers + 2 atoms (keys, size) in `src/quickjs/quickjs-atom.h`/`quickjs.c`. One conflict in `js_map_delete` resolved by following upstream's `map_normalize_key` → `map_normalize_key_const` rename. Adapted 6 `JS_ThrowTypeError(ctx, fmt)` call sites to the fork's `(ctx, file, line, fmt)` signature. Fixed two upstream-bug issues found during port: (1) `js_set_symmetricDifference` and `js_set_difference` were violating `map_delete_record`'s "remove from hash table first" contract, leading to a hash-chain self-cycle when `map_add_record` reused the freed slot (infinite loop, manifested on `staging/sm/Set/symmetric-difference.js`); fixed by adding a `map_unlink_record` helper called before `map_delete_record`. (2) Several callsites passed owned `item` to `map_add_record` (which always dups), but never freed `item` on the success path, leaking; also two callsites wrapped the input in a redundant `JS_DupValue` causing further leaks. Fixed by freeing `item` after successful add and removing the redundant dups. Removed 7 `staging/sm/Set/*.js` excludes from `test262.conf` per upstream; 5 of those tests now fail on the fork on IteratorClose-related spec edge cases (upstream's code doesn't call `return` on iterators on early exit) — these are recorded in `test262_errors.txt`. Updated 5 bytecode-test inline snapshots in `tests/file-to-bytecode.test.ts`, `tests/libbytecode.test.ts`, `tests/qjsbootstrap-bytecode.test.ts` for legitimate atom-ID drift from the new atoms. `TODO` not vendored. |
| 0cef7f0 | PORT | set methods: removed memory leaks - fixed ordering of property access - fixed conversion to integer of 'size' in GetSetRecord() - added missing iterator close - factorized code | Upstream's own fix for the bugs I noticed when porting a1e073e: factorizes property access into `get_set_record`, introduces `map_delete_record_internal` + high-level `map_delete_record(ctx, s, key)`, introduces `set_add_record` + `js_copy_set` helpers, adds missing iterator close (`return` call). Fork-side adjustments: my `map_unlink_record` is obsolete (deleted); my JS_FreeValue-after-add fixes are dropped (upstream's restructure subsumes them); JS_ThrowTypeError/JS_ThrowRangeError calls adapted to fork's `(ctx, file, line, fmt)` signature. test262 errors 59 → 54 — the 5 IteratorClose tests from a1e073e are fixed by this commit's "added missing iterator close". `TODO` not vendored. |
| 0377dab | PORT | removed uninitialized values - removed useless init | Cleanup in `src/quickjs/quickjs.c` - removed uninitialized-value warnings in set methods. One conflict (one-line block missing in fork) resolved by taking upstream. |
| 982b7aa | PORT | added the Iterator object | Added the `Iterator` global constructor in `src/quickjs/quickjs.c` plus the `JS_CLASS_ITERATOR` enum entry (placed in the fork's quickjs.h since the fork moved this enum there from quickjs.c). Renamed `iterator_proto` to `iterator_ctor` in JSContext. Adapted JS_ThrowTypeError to fork's 4-arg signature. Regenerated 6 snapshot tests for the new Iterator global appearing in the snapshot output (globals, libcontext, libbytecode (×3), file-to-bytecode, qjsbootstrap-bytecode for atom-ID drift). |
| e924173 | PORT | added Iterator.prototype.toArray and Iterator.from (bnoordhuis) | Added Iterator.from and Iterator.prototype.toArray in `src/quickjs/quickjs.c`. New `JS_CLASS_ITERATOR_WRAP` enum entry placed in fork's quickjs.h. Adapted JS_ThrowTypeError to fork's 4-arg signature. Regenerated bytecode-test snapshots for atom-ID drift. |
| b2ed2e9 | PORT | added Iterator.prototype.[drop,filter,flatMap,map,take,every,find,forEach,some,reduce,[Symbol.toStringTag]] (saghul) | Added Iterator helper methods plus the supporting `js_iterator_helper_*` machinery in `src/quickjs/quickjs.c`. New `JS_CLASS_ITERATOR_HELPER` enum entry placed in fork's quickjs.h. Adapted 5 JS_ThrowTypeError/JS_ThrowRangeError calls to fork's 4-arg signature. Added `staging/sm/Iterator` exclude in `test262.conf`. Note: this commit's code has known leak/spec-compliance issues that are fixed by the immediate follow-ups 3dcca0d, cf0e179, and 2d99c32; the four ports must be applied together for green test262 (the GC `list_empty` assertion fires partway through if 2d99c32 isn't yet applied). |
| 3dcca0d | PORT | fix Iterator.prototype.constructor (initial patch by bnoordhuis) | Small Iterator.prototype.constructor fix in `src/quickjs/quickjs.c`. Applied cleanly. |
| cf0e179 | PORT | Iterator is an abstract class (bnoordhuis) | Made Iterator constructor abstract (throws "abstract class not constructable" when called directly via its C function). One conflict in `js_iterator_constructor` resolved by taking upstream's new structure and re-applying the fork's 4-arg JS_ThrowTypeError signature. |
| 2d99c32 | PORT | Iterator functions: removed memory leak / don't close iterator when IteratorStepValue fails / fixed Iterator Wrap next() and return() arguments / fixed Iterator.from() | Upstream's own fix for the leaks and spec gaps in b2ed2e9. One conflict in `js_iterator_from` resolved by following upstream's restructure (combined isObject/isString check, drop the proto chain walk + JS_OrdinaryIsInstanceOf check that was apparently temporary). `staging/sm/Iterator/from/wrap-next-not-object-throws.js` added to excludes in `test262.conf` (upstream says it doesn't match spec). `TODO` not vendored. test262 stays at 54 errors with no GC assertion. |
| 1e19893 | PORT | - added ArrayBuffer.prototype.transfer (Divy Srivastava) - fixed transfer when the ArrayBuffer was allocated with a custom allocator | Added `ArrayBuffer.prototype.transfer` and `transferToFixedLength` plus `.detached` getter in `src/quickjs/quickjs.c`. Adapted 3 JS_ThrowTypeError/JS_ThrowRangeError to fork's 4-arg signature. |
| 44d03a8 | PORT | fixed parsing of computed property name | One-line fix in `src/quickjs/quickjs.c`. test262 errors 54 → 53. |
| f021d77 | PORT | - added resizable array buffers (bnoordhuis) - fixed Atomics.wait, Atomics.notify, TypedArray.prototype.lastIndexOf - fixed JS_PreventExtensions() with resizable typed arrays | Large patch (~1000 lines net in `src/quickjs/quickjs.c`) adding resizable array buffers + RAB-aware TypedArray. New atom `maxByteLength` added. 7 conflicts resolved: `get_typed_array` signature change (removed `is_dataview` param + DataView branch); `js_typed_array_set_internal` `typed_array_is_detached` → `typed_array_is_oob` switch; `js_typed_array_sort` rename `get_length_internal` → `get_length_unsafe` + restored fork's detailed cmp-error message; `js_atomics_get_ptr` signature `void*` → `int` with out-param `pptr`; merged DataView getter check (kept fork's pos+size bounds check, added upstream's TypeError for ta->offset+length>byte_length). Adapted 13 JS_ThrowTypeError/JS_ThrowRangeError/JS_ThrowInternalError to fork's 4-arg signature. `tests/test_bjson.js` not vendored. test262 errors 53 → 50 (3 set-detached tests now pass). |
| 8807fed | PORT | - added Atomics.pause (bnoordhuis) - use the pause instruction for x86 and ARM64 in Atomics.pause() | Added `Atomics.pause` in `src/quickjs/quickjs.c` (uses x86 `pause` and ARM64 `isb` instructions). Adapted 1 JS_ThrowTypeError to fork's 4-arg signature. |
| 456e016 | PORT | added Map and WeakMap upsert methods (bnoordhuis) | Added `Map.prototype.upsert` / `WeakMap.prototype.upsert` in `src/quickjs/quickjs.c`. Adapted 2 JS_ThrowTypeError to fork's 4-arg signature. `TODO` not vendored. |
| bc753c6 | PORT | added Math.sumPrecise() | Added `Math.sumPrecise()` in `src/quickjs/quickjs.c`. Adapted 1 JS_ThrowTypeError; added the test case to `tests/oldtests/test_builtin.js`. `TODO` not vendored. |
| a6db749 | PORT | fixed Atomics.pause() in the NaN boxing case (32 bit cpu) | One-line fix in `src/quickjs/quickjs.c`. |
| 0060876 | PORT | fixed Date parsing: "1997-03-08 11:19:10-0700" is a valid date and "1997-03-08T11:19:10-07" should yield an error | Date-parsing fix in `src/quickjs/quickjs.c`. test262 errors 50 → 49. `TODO` not vendored. |
| c3e5ae2 | PORT | simplified math.sumPrecise() | Upstream refactor of `sum_precise_add` and `sum_precise_get_result` in `src/quickjs/quickjs.c` — simpler algorithm using a renorm-on-counter pattern. 4 conflicts resolved by taking upstream's version. |
| fb14cc6 | PORT | Run test262 tests in CI | The CI workflow + Makefile changes are SKIP-NA (fork's CI is different, no Makefile). The vendored bits applied: `immutable-arraybuffer=skip` added to `src/run-test262/test262.conf` (one conflict resolved keeping fork's `host-gc-required=skip` and `import-assertions` lines too); atomicsHelper timeouts doubled in `src/run-test262/test262.patch` (yield 20→40, small 20→40, long 100→200). |
| 4af5b1e | SKIP-DONE | Merge pull request #408 from nickva/run-test262-in-ci | Merge commit pulling fb14cc6 (already ported in previous commit). |
| 31663a9 | PORT | updated test262 | Applied test262.conf additions (host-gc-required, nonextensible-applies-to-private=skip, stable-array-sort, stable-typedarray-sort); one conflict resolved by taking upstream's `host-gc-required` (no =skip). Applied the index-hash refresh in `tests/test262.patch` (kept fork's hash on atomicsHelper since fork uses different timeouts; dropped upstream's removal of fork's existing patch hunks for harness/sm/non262.js, regress-469625-01.js, new-with-non-constructor.js — those hunks still apply at the fork's test262 commit). Makefile + TODO not vendored. Upstream's test262_errors.txt diff doesn't apply (different commit). |
| f1253f2 | PORT | Improve error handling in Promise.withResolvers (bnoordhuis) | Error-handling cleanup in `src/quickjs/quickjs.c`. |
| f4951ef | PORT | optimize the create of arrays - optimized the rest and array_from opcodes | Optimization of array creation in `src/quickjs/quickjs.c`. Bytecode-test snapshot regenerated for shape drift. |
| 2c90110 | PORT | - optimized global variable access | Optimized global variable access in `src/quickjs/quickjs.c` and `src/quickjs/quickjs-opcode.h`. Upstream's commit message: "removed full compliance with the spec for strict mode variable assignment so that they are as fast as in non strict mode (V8, SpiderMonkey and JavascriptCore do the same)" — this regresses `language/identifier-resolution/assign-to-global-undefined.js` (49 → 50 errors), matching upstream's own intentional change. Bytecode-test snapshots regenerated for opcode renumbering. `tests/microbench.js` not vendored. |
| 57f8ec0 | PORT | inlined fast path for get_field, get_field2 and put_field | Optimization in `src/quickjs/quickjs.c`. |
| 64c55c6 | PORT | removed JS_PROP_NO_ADD | Cleanup in `src/quickjs/quickjs.c` and `src/quickjs/quickjs.h`. |
| 8e8eefb | PORT | optimized array access by inlining get_array_el, get_array_el2, get_array_el3 and put_array_el | Optimization in `src/quickjs/quickjs.c`: macro-ized OP_get_field/OP_get_field2 (`GET_FIELD_INLINE`); added array-fast-path to OP_get_array_el/OP_get_array_el2 (`GET_ARRAY_EL_INLINE`), OP_get_array_el3 (also fixes the `switch(sp[-2])` → `switch(sp[-1])` property-key check), and OP_put_array_el. Preserved fork's `JS_ThrowTypeError(ctx, "<internal>/quickjs.c", __LINE__, ...)` signature in OP_get_array_el3. Also added `array_update` benchmark to `meta/microbench.js`. Earlier attempt in this session (note in commit a99c91d) was prematurely rolled back due to `tests/worker-onerror.test.ts` *appearing* to hang — actually just slow under wine cold-start (~3min for 41 tests including 21 wine variants). Tests all pass green when given enough time. |
| e5de89f | PORT | optimized post_inc and post_dec | Optimization in `src/quickjs/quickjs.c`. Applied cleanly via 3-way merge. |
| 79f3ae2 | PORT | optimized string_buffer_putc() | Optimization in `src/quickjs/quickjs.c`. Applied cleanly. Required snapshot update in `tests/qjsbootstrap-bytecode.test.ts` (line number 7423 → 7443 due to added code). |
| c8a8cf5 | PORT | faster appending of elements in arrays | Optimization in `src/quickjs/quickjs.c`. Applied cleanly. Snapshot update in `tests/qjsbootstrap-bytecode.test.ts` (line 7443 → 7447). |
| 7a488f3 | SKIP-NA | update | Changelog + doc/quickjs.texi only; fork doesn't vendor either. |
| 42eb279 | PORT | Faster context creation and exception checks in JS_NewContext (#404) | Large refactor in `src/quickjs/quickjs.c` + `src/quickjs/quickjs.h`. Restructures `JS_AddIntrinsicBasicObjects` / `JS_AddIntrinsicBaseObjects`: Error/native errors and Array now created in BasicObjects (was BaseObjects). Function/Error init now uses new `JS_NewCConstructor` consolidated helper (replaces fork's older `JS_NewCFunctionMagic` + `JS_NewGlobalCConstructor2` + `JS_SetPropertyFunctionList` triple). All `JS_AddIntrinsic*` functions now return `int` (was `void`) so `JS_NewContext()` can check exceptions. Added `JS_DEF_PROP_SYMBOL` / `JS_DEF_PROP_BOOL` macros. Adapted 4 conflicts in init code: dropped fork's older API in favor of upstream's new API, preserved fork's `Error.length=1` fix (it lives in upstream's new code too via `n_args = 1 + (i == JS_AGGREGATE_ERROR)`). FORK-SPECIFIC FIX: upstream's new init uses `JS_DEF_PROP_STRING` for native error `.name`/`.message`, which materializes lazily via `JS_DefineAutoInitProperty`. The fork's `js_print_error` uses `get_prop_string` (no-side-effect lookup) which can't trigger autoinit and so falls back to "Error" for all subclasses on first print. Added eager `JS_GetProperty` calls right after `JS_NewCConstructor` for Error.prototype + each native_error_proto to force materialization. Snapshot updates in 3 tests reflect new global ordering (Error/Array now before Object/Function/Iterator). Worker-exit test was a one-time flake under wine, passed on retry. |
| eb9fa2b | PORT | compilation fix for clang | Compiler fix in `src/quickjs/quickjs.c`. Applied cleanly. |
| 6345009 | PENDING | fixed regression in error message display introduced in commit 42eb279 |  |
| 2a53de0 | PENDING | test262 update |  |
| 0d4cd2d | PENDING | faster and safer dbuf functions (#443) |  |
| 7fb994c | PENDING | fixed argument evaluation order in Date constructor and Date.UTC() |  |
| 2161640 | PENDING | stricter year parsing in Date |  |
| c720e35 | PENDING | added js_string_eq() |  |
| 3e5f2bb | PENDING | inlined the get_length operation |  |
| 9a421b3 | PENDING | optimized Array.prototype.push |  |
| af16a97 | PENDING | changed module rejection order according to spec change |  |
| c31809e | PENDING | fixed operation order in Regexp constructor |  |
| eab6945 | PENDING | updated test results |  |
| eb2c890 | PENDING | removed uninitialized variable |  |
| a6816be | PENDING | optimized global variable access |  |
| e015918 | PENDING | Much faster destructuring at the expense of a slight incompatibility with the spec when direct evals are present (v8 behaves the same way). |  |
| 961478d | PENDING | removed duplicate test |  |
| baa186f | PENDING | qjs: added --strict option - don't consider included files as modules - allow module and strict code with -e option |  |
| 75b5230 | PENDING | Fix use-after-free in ArrayBuffer.prototype.transfer (bnoordhuis) (#450) - use js_array_buffer_update_typed_arrays() in JS_DetachArrayBuffer() |  |
| 7cfddd0 | PENDING | fixed DataView resizing |  |
| c6fe5a9 | PENDING | Fix length check in ArrayBuffer.prototype.slice (bnoordhuis) (#451) |  |
| 080c01f | PENDING | More informative "not a constructor" error message (initial patch by bnoordhuis) (#368) |  |
| d10613f | PENDING | fixed exception handling in put_var operation (regression introduced by commit a6816be) (#454) |  |
| b07ad11 | PENDING | fixed JS_PROP_AUTOINIT handling in js_closure_define_global_var() (#455) |  |
| 9688007 | PENDING | Restore a mistakenly removed goto on error in js_build_module_ns() (igorburago) |  |
| ae7219b | PENDING | - Closure optimization (go from quadratic to linear time when the number of closure variables is large) - Separated JSVarDef and JSBytecodeVarDef to simplify the code and save memory - fixed debug info stripping with global variables |  |
| 125b012 | PENDING | added error checking in JS_InstantiateFunctionListItem() |  |
| 3d0cc29 | PENDING | optimized add/sub int32 overflow |  |
| 4bd485d | PENDING | - Added Iterator.concat (initial patch by bnoordhuis) - optimized js_iterator_concat_next() - added more guards against recursion in Iterator.concat operations |  |
| fcbf5ea | PENDING | fixed BJSON array serialization (#457) |  |
| 7ab2341 | PENDING | faster and simpler implementation of regexp backtracking |  |
| 9f11034 | PENDING | - optimized Regexp.prototype.exec - optimized String.prototype.replace - optimized 'arguments' object creation - optimized access to non strict 'arguments' elements |  |
| 728ed94 | PENDING | fixed Worker freeing logic (#462) |  |
| a774007 | PENDING | removed buffer overflows introduced in regexp optimizations |  |
| 371c06e | PENDING | regexp: ensure that the bytecode size grows linearly with respect to the input regexp. |  |
| 47aac8b | PENDING | regexp: cosmetic: make it clearer that there is now a set of registers instead of an auxiliary stack |  |
| 5907aa6 | PENDING | added missing lre_poll_timeout() |  |
| b226856 | PENDING | updated to unicode 17.0.0 - updated test262 version |  |
| 24379bf | PENDING | added regexp duplicate named groups - fixed reset of captures with quantizers |  |
| e5fd391 | PENDING | fixed fast array extension optimization when there are multiple realms |  |
| fcd33c1 | PENDING | removed memory leak in case of error in cpool_add() (#468) |  |
| 1dbba8a | PENDING | removed use after free in js_create_module_bytecode_function() (#467) |  |
| c73a435 | PENDING | Don't call well-known Symbol methods for RegExp on primitive values |  |
| 31ef02b | PENDING | slightly faster lexical variable assignment |  |
| 7bd1ae2 | PENDING | \x{N} is a syntax error |  |
| f113949 | PENDING | regexp: removed alloca() is lre_exec() - added specific opcodes for \s and \S to have a smaller bytecode - optimized \b and \B |  |
| 4c722ce | PENDING | modified js_allocate_fast_array() so that the array is fully initialized. It is slightly slower but avoids several nasty bugs (#471) |  |
| 0989d4c | PENDING | fixed TypedArray sort semantics by copying the array before calling the comparison function. Fixed buffer overflow when the array is resized (#477) |  |
| 68caa5f | PENDING | fixed TypedArray constructor semantics which removes a buffer overflow (#478) |  |
| aaf0174 | PENDING | test262 update |  |
| 69090b9 | PENDING | Fix stack underflow with generator in iterable (saghul) (#488) |  |
| 841dd03 | PENDING | fixed buffer overflow in TypedArray.prototype.with (#492) |  |
| f1b63fc | PENDING | Fix memory leak in Iterator.prototype.map (saghul) (#493) |  |
| 16d6947 | PENDING | typo |  |
| 46bd985 | PENDING | fixed buffer overflow in Atomics with resizable typed arrays |  |
| 4d16546 | PENDING | fixed RegExp.escape |  |
| e7b9f21 | PENDING | Fix async generator lifecycle bug (bnoordhuis) (quickjs-ng/quickjs#1355) |  |
| 5022f2b | PENDING | fixed use-after-free via re-entrant GC in FinalizationRegistry weak reference cleanup (#494) |  |
| a31dcef | PENDING | added basic protection against too large function in serialized bytecode |  |
| d7ae12a | PENDING | added JSON.parse source text access |  |
